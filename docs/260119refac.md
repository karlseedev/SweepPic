# FaceComparisonViewController 리팩토링 계획

## 개요

- **대상 파일**: `PickPhoto/PickPhoto/Features/SimilarPhoto/UI/FaceComparisonViewController.swift`
- **현재 줄 수**: 1670줄
- **목표 줄 수**: 각 파일 1000줄 이하
- **목표**: 파일 크기 감소 + 캡슐화 (의존성 주입까지)
- **진행 방식**: 3단계로 나눠서 진행 (안전성 확보)

---

## 현재 파일 구조 분석

| 구분 | 라인 범위 | 줄 수 | 분류 |
|------|-----------|-------|------|
| 메인 VC 핵심 로직 | 1-625 | ~625줄 | 유지 |
| 디버그: 감지 비교 | 627-891 | ~265줄 | **1차 분리** |
| 디버그: 정보 생성 | 893-1023 | ~131줄 | **1차 분리** |
| CollectionView Extensions | 1026-1223 | ~198줄 | 유지 |
| FaceComparisonCell | 1230-1360 | ~131줄 | 2차 분리 |
| FaceComparisonHeaderView | 1367-1433 | ~67줄 | **2차 삭제** |
| FaceComparisonTitleBar | 1440-1594 | ~155줄 | 2차 분리 |
| 디버그 구조체들 | 1596-1669 | ~74줄 | **1차 분리** |

---

## 1차: 디버그 코드 분리

### 목표
- 릴리즈에 필요 없는 디버그 코드를 별도 파일로 분리
- `#if DEBUG`로 감싸서 릴리즈 빌드에서 제외
- **디버그 버튼도 릴리즈에서 숨김**
- **나중에 파일 하나만 삭제하면 끝**

### 생성 파일
`PickPhoto/PickPhoto/Features/SimilarPhoto/Debug/FaceComparisonDebug.swift`

### 설계 원칙

```swift
// FaceComparisonDebug.swift 전체를 #if DEBUG로 감싸기
#if DEBUG

import UIKit
import Photos
import Vision
import AppCore

// 디버그 헬퍼 - 단순 이동
enum FaceComparisonDebugHelper {
    /// 그룹 매칭 시뮬레이션 + 캐시 상태 출력
    static func runGroupMatchingTest(
        assetIDs: [String],
        photos: [PHAsset],
        groupID: String,
        validPersonIndices: [Int],
        currentPersonIndex: Int
    ) async { ... }

    /// Extended Fallback 테스트
    static func runExtendedFallbackTest(
        assetIDs: [String],
        photos: [PHAsset]
    ) async { ... }

    /// Vision fallback ON/OFF 비교
    static func runVisionFallbackComparison(
        assetIDs: [String],
        photos: [PHAsset]
    ) async { ... }

    /// YuNet vs Vision 비교
    static func runDetectionComparison(with photos: [PHAsset]) async { ... }

    /// 얼굴 매칭 (위치 기반)
    static func matchDetectedFaces(...) -> DetectionMatchResult { ... }

    /// 디버그 정보 생성
    static func generateDebugInfo(
        assetIDs: [String],
        groupID: String,
        validPersonIndices: [Int],
        currentPersonIndex: Int
    ) async -> FaceDebugInfo { ... }

    /// 디버그 정보 출력
    static func printDebugInfo(_ info: FaceDebugInfo) { ... }
}

// 디버그 구조체들
struct DebugReferenceSlot { ... }
struct DetectionMatchResult { ... }
struct FaceDebugInfo: Codable { ... }
struct FaceDebugSlot: Codable { ... }
struct PhotoDebugInfo: Codable { ... }
struct FaceDebugEntry: Codable { ... }

#endif
```

### 분리 대상

#### 1. 디버그 헬퍼 메서드들
- `debugButtonTapped` 내부 로직 → `runGroupMatchingTest()`
- `extendedTestButtonTapped` 내부 로직 → `runExtendedFallbackTest()`
- `compareDetectionButtonTapped` 내부 로직 → `runVisionFallbackComparison()`
- `runDetectionComparison()` (라인 627-739)
- `runVisionFallbackComparison()` (라인 741-817)
- `matchDetectedFaces()` (라인 830-891)
- `generateDebugInfo()` (라인 903-1004)
- `printDebugInfo()` (라인 1007-1023)

#### 2. 디버그 구조체들
- `DebugReferenceSlot` (라인 896-899)
- `DetectionMatchResult` (라인 1599-1606)
- `FaceDebugInfo` (라인 1611-1628)
- `FaceDebugSlot` (라인 1631-1638)
- `PhotoDebugInfo` (라인 1641-1646)
- `FaceDebugEntry` (라인 1649-1669)

### 메인 파일 수정 사항

#### 1. 디버그 버튼 노출 제어 (iOS 26+)

```swift
// setupSystemNavigationBar()
@available(iOS 26.0, *)
private func setupSystemNavigationBar() {
    updateNavigationTitle()

    // 순환 버튼 (항상 표시)
    let cycleButton = UIBarButtonItem(
        image: UIImage(systemName: "arrow.trianglehead.2.clockwise.rotate.90"),
        style: .plain,
        target: self,
        action: #selector(cycleButtonTapped)
    )
    cycleButton.tintColor = .white

    #if DEBUG
    // 디버그 버튼들 (DEBUG 빌드에서만 표시)
    let extendedTestButton = UIBarButtonItem(
        image: UIImage(systemName: "sparkle.magnifyingglass"),
        style: .plain,
        target: self,
        action: #selector(extendedTestButtonTapped)
    )
    extendedTestButton.tintColor = .systemCyan

    let debugButton = UIBarButtonItem(
        image: UIImage(systemName: "ladybug"),
        style: .plain,
        target: self,
        action: #selector(debugButtonTapped)
    )
    debugButton.tintColor = .systemOrange

    navigationItem.rightBarButtonItems = [cycleButton, debugButton, extendedTestButton]
    #else
    navigationItem.rightBarButtonItems = [cycleButton]
    #endif
}
```

#### 2. 디버그 버튼 노출 제어 (iOS 16~25, TitleBar)

```swift
// FaceComparisonTitleBar.setupUI()
private func setupUI() {
    addSubview(blurView)
    addSubview(contentView)
    contentView.addSubview(closeButton)
    contentView.addSubview(titleLabel)
    contentView.addSubview(cycleButton)

    #if DEBUG
    contentView.addSubview(extendedTestButton)
    contentView.addSubview(debugButton)
    #endif

    // 제약조건도 #if DEBUG로 분기
    // ...
}
```

#### 3. 액션 메서드 수정 (로직 위임)

```swift
// debugButtonTapped - 로직을 헬퍼로 위임
@objc private func debugButtonTapped() {
    #if DEBUG
    Task { @MainActor in
        let assetIDs = comparisonGroup.selectedAssetIDs

        // PHAsset 변환 (순서 보장을 위해 assetIDs 순서대로)
        let fetchResult = PHAsset.fetchAssets(
            withLocalIdentifiers: assetIDs,
            options: nil
        )
        var photosDict: [String: PHAsset] = [:]
        fetchResult.enumerateObjects { asset, _, _ in
            photosDict[asset.localIdentifier] = asset
        }
        let photos = assetIDs.compactMap { photosDict[$0] }

        await FaceComparisonDebugHelper.runGroupMatchingTest(
            assetIDs: assetIDs,
            photos: photos,
            groupID: comparisonGroup.sourceGroupID,
            validPersonIndices: validPersonIndices,
            currentPersonIndex: currentPersonIndex
        )
    }
    #endif
}

// extendedTestButtonTapped - 로직을 헬퍼로 위임
@objc private func extendedTestButtonTapped() {
    #if DEBUG
    Task { @MainActor in
        let assetIDs = comparisonGroup.selectedAssetIDs

        let fetchResult = PHAsset.fetchAssets(
            withLocalIdentifiers: assetIDs,
            options: nil
        )
        var photosDict: [String: PHAsset] = [:]
        fetchResult.enumerateObjects { asset, _, _ in
            photosDict[asset.localIdentifier] = asset
        }
        let photos = assetIDs.compactMap { photosDict[$0] }

        await FaceComparisonDebugHelper.runExtendedFallbackTest(
            assetIDs: assetIDs,
            photos: photos
        )
    }
    #endif
}

// compareDetectionButtonTapped - 로직을 헬퍼로 위임
@objc private func compareDetectionButtonTapped() {
    #if DEBUG
    Task { @MainActor in
        let assetIDs = comparisonGroup.selectedAssetIDs

        let fetchResult = PHAsset.fetchAssets(
            withLocalIdentifiers: assetIDs,
            options: nil
        )
        var photosDict: [String: PHAsset] = [:]
        fetchResult.enumerateObjects { asset, _, _ in
            photosDict[asset.localIdentifier] = asset
        }
        let photos = assetIDs.compactMap { photosDict[$0] }

        await FaceComparisonDebugHelper.runVisionFallbackComparison(
            assetIDs: assetIDs,
            photos: photos
        )
    }
    #endif
}
```

### 1차 완료 후 예상 결과

| 파일 | 줄 수 |
|------|-------|
| FaceComparisonViewController.swift | ~1200줄 |
| FaceComparisonDebug.swift | ~470줄 |

### 1차 체크리스트

- [ ] Debug 폴더 생성 (`PickPhoto/PickPhoto/Features/SimilarPhoto/Debug/`)
- [ ] FaceComparisonDebug.swift 생성
- [ ] `#if DEBUG` ~ `#endif`로 전체 감싸기
- [ ] import UIKit, Photos, Vision, AppCore 추가
- [ ] FaceComparisonDebugHelper enum 생성
- [ ] 디버그 메서드들 이동 (assetIDs 순서 보장 주의)
- [ ] 디버그 구조체들 이동
- [ ] 메인 파일: setupSystemNavigationBar() 버튼 #if DEBUG 처리
- [ ] 메인 파일: FaceComparisonTitleBar 버튼 #if DEBUG 처리
- [ ] 메인 파일: 3개 액션 메서드에 #if DEBUG 추가
- [ ] 빌드 확인 (Debug/Release 둘 다)
- [ ] 커밋

---

## 2차: UI 컴포넌트 분리

### 목표
- 독립적인 UI 컴포넌트를 별도 파일로 분리
- 단순 이동, 의존성 없음

### 생성 파일
`PickPhoto/PickPhoto/Features/SimilarPhoto/UI/FaceComparisonViews.swift`

### 분리 대상

#### 1. FaceComparisonTitleBarDelegate (프로토콜)
- 내용:
  ```swift
  protocol FaceComparisonTitleBarDelegate: AnyObject {
      func faceComparisonTitleBarDidTapCycle(_ titleBar: FaceComparisonTitleBar)
      func faceComparisonTitleBarDidTapClose(_ titleBar: FaceComparisonTitleBar)
      #if DEBUG
      func faceComparisonTitleBarDidTapDebug(_ titleBar: FaceComparisonTitleBar)
      func faceComparisonTitleBarDidTapExtendedTest(_ titleBar: FaceComparisonTitleBar)
      #endif
  }
  ```

#### 2. FaceComparisonTitleBar (클래스)
- 줄 수: ~148줄
- 의존성: `FaceComparisonTitleBarDelegate` (같이 이동)
- 주의: 디버그 버튼 관련 코드는 `#if DEBUG`로 감싸야 함
  - 버튼 생성 (setupUI 내부)
  - @objc 액션 메서드 (`debugButtonTapped`, `extendedTestButtonTapped`)
  - delegate 호출 (`delegate?.faceComparisonTitleBarDidTapDebug(self)` 등)

#### 3. FaceComparisonCell (클래스)
- 줄 수: ~131줄
- 의존성: 없음 (UICollectionViewCell 상속)

### 삭제 대상

#### FaceComparisonHeaderView
- **삭제 사유**:
  - 초기 구현(bd20288)에서 TitleBar와 HeaderView 둘 다 존재
  - d58fd73 커밋에서 "불필요한 컬렉션뷰 헤더 제거 (별도 타이틀바 사용)"로 `.zero` 처리
  - TitleBar가 상단 고정으로 같은 역할을 하므로 중복
  - 코드만 남고 실제 미사용 상태
- 관련 코드 정리:
  - `collectionView.register()` 호출 제거
  - `viewForSupplementaryElementOfKind` 메서드 제거
  - `referenceSizeForHeaderInSection` 메서드 제거

### 2차 완료 후 예상 결과

| 파일 | 줄 수 |
|------|-------|
| FaceComparisonViewController.swift | ~850줄 |
| FaceComparisonViews.swift | ~280줄 |
| FaceComparisonDebug.swift | ~470줄 |

### 2차 체크리스트

- [ ] FaceComparisonViews.swift 생성 (`PickPhoto/PickPhoto/Features/SimilarPhoto/UI/`)
- [ ] import UIKit 추가 (Photos 불필요)
- [ ] FaceComparisonTitleBarDelegate 이동 (#if DEBUG 유지)
- [ ] FaceComparisonTitleBar 이동 (#if DEBUG 유지)
- [ ] FaceComparisonCell 이동
- [ ] FaceComparisonHeaderView 삭제
- [ ] 관련 등록/delegate 코드 정리
- [ ] 빌드 확인
- [ ] 커밋

---

## 3차: 유지 코드 캡슐화

### 목표
- 유지할 코드의 경계와 의존성 정리
- 의존성 주입을 통한 테스트 가능성 향상
- swipe 구현 시 `FaceComparisonDataSource` 프로토콜과 통합

### 대상
- `FaceComparisonViewController` 내부 의존성 정리
- 싱글톤 직접 접근 → 프로토콜 주입으로 변경

### 설계 원칙

```swift
// 의존성 주입을 위한 프로토콜
protocol SimilarityCacheProtocol {
    func getFaces(for assetID: String) async -> [CachedFace]
    func getGroupValidPersonIndices(for groupID: String) async -> Set<Int>
}

extension SimilarityCache: SimilarityCacheProtocol {}

// ViewController에서 의존성 주입
final class FaceComparisonViewController: UIViewController {
    private let cache: SimilarityCacheProtocol

    init(
        comparisonGroup: ComparisonGroup,
        fetchResult: PHFetchResult<PHAsset>?,
        trashStore: TrashStoreProtocol = TrashStore.shared,
        cache: SimilarityCacheProtocol = SimilarityCache.shared  // 의존성 주입
    ) { ... }
}
```

### 3차 체크리스트

- [ ] SimilarityCacheProtocol 정의
- [ ] SimilarityCache에 프로토콜 extension 추가
- [ ] FaceComparisonViewController init에 cache 파라미터 추가
- [ ] 내부 SimilarityCache.shared 호출을 cache로 변경
- [ ] swipe 구현 시 FaceComparisonDataSource와 통합 검토
- [ ] 빌드 확인
- [ ] 커밋

### 3차 완료 후 예상 결과

| 파일 | 줄 수 | 상태 |
|------|-------|------|
| FaceComparisonViewController.swift | ~850줄 | 캡슐화 완료 |
| FaceComparisonViews.swift | ~280줄 | UI 분리 완료 |
| FaceComparisonDebug.swift | ~470줄 | 삭제 대기 |

---

## 최종 파일 구조

```
PickPhoto/PickPhoto/Features/SimilarPhoto/
├── UI/
│   ├── FaceComparisonViewController.swift  (~850줄, 핵심 로직)
│   └── FaceComparisonViews.swift           (~280줄, UI 컴포넌트)
└── Debug/
    └── FaceComparisonDebug.swift           (~470줄, #if DEBUG)
```

---

## 진행 요약

| 단계 | 목표 | 핵심 작업 |
|------|------|-----------|
| **1차** | 디버그 분리 | #if DEBUG로 감싸서 분리, **버튼도 숨김**, 삭제 용이 |
| **2차** | UI 분리 | Cell, TitleBar 분리, HeaderView 삭제 |
| **3차** | 캡슐화 | 의존성 주입, 프로토콜 추상화 |

---

## 리스크 및 대응

| 리스크 | 대응 |
|--------|------|
| Selector 접근 불가 | 액션 메서드는 메인 파일에 유지, #if DEBUG로 로직 감싸기 |
| 릴리즈에 디버그 버튼 노출 | 버튼 생성부도 #if DEBUG로 감싸기 |
| PHAsset 순서 불일치 | assetIDs 순서대로 photos 배열 생성 |
| 릴리즈 빌드 오류 | 각 단계마다 Debug/Release 둘 다 빌드 확인 |
| 빌드 실패 | 각 단계마다 빌드 확인 후 커밋 |
| 기능 오류 | 각 단계마다 앱 실행 테스트 |

---

## 향후 고려사항

- 출시 전 `FaceComparisonDebug.swift` 파일 삭제
- 메인 파일의 `#if DEBUG` 블록도 함께 삭제
- swipe 구현 시 3차 캡슐화와 통합 진행
