# PickPhoto PRD v4

## 0. 문서 정보

| 항목 | 내용 |
|------|------|
| 제품명 | PickPhoto |
| 목적 | iOS 기본 사진 앱과 유사한 "매우 빠른" 사진 브라우징 경험 제공 |
| 문서 성격 | 제품 요구사항 (PRD) + 성능/품질 수용 기준 |
| 상태 | 기획 확정 (구현 전) |
| iOS 버전 | iOS 16+ |
| 최상위 타깃 | iPhone 17 Pro (ProMotion 120Hz) |
| 최저 타깃 | iPhone 12 |

---

## 1. 배경/문제 정의

iOS 기본 사진 앱은 대규모 라이브러리에서도 다음을 제공합니다:
- 빠른 실행
- 즉시성 있는 썸네일 채움
- 매우 부드러운 스크롤 (특히 ProMotion)
- 자연스러운 전환/제스처

**PickPhoto는 가능한 한 동일한 공개 API(주로 PhotoKit) 기반으로 이 체감 품질을 달성하는 것을 목표로 합니다.**

추가로, 위 스와이프 삭제 등 "사진 정리에 특화된" UX를 제공하여 기본 앱 대비 차별점을 만듭니다.

---

## 2. 목표 (Goals)

### 2.1 핵심 UX 목표

- All Photos 그리드가 "즉시 열리고", 스크롤이 "매우 부드럽다"고 느껴지게 합니다.
- 핀치 제스처로 그리드 밀도가 자연스럽게 변화합니다 (Photos와 유사한 감성).
- 앨범 리스트/앨범 진입에서도 동일한 그리드/줌 경험을 제공합니다.
- 그리드와 뷰어에서 모두 삭제가 가능하며, 삭제 후 흐름이 끊기지 않습니다.

### 2.2 성능/품질 목표 (측정 가능한 KPI)

아래 수치는 **초기 목표치**이며, 스파이크(실험) 및 계측 결과로 조정될 수 있습니다.

| 항목 | iPhone 17 Pro | iPhone 12 | 정의/측정 방법 |
|------|---------------|-----------|----------------|
| 스크롤 체감 | 120Hz급 (끊김 거의 없음) | 60Hz급 (끊김 최소) | 사용자 체감 + hitch 비율 |
| 스크롤 hitch 비율 | < 1% | < 2% | Instruments Core Animation으로 측정 |
| 첫 그리드 유효 썸네일 | < 300ms | < 600ms | placeholder → 실 이미지 대체 시점 |
| 썸네일 응답 시간 (p50) | < 100ms | < 150ms | 셀 요청 → 표시 (Instruments 측정) |
| 오표시 (정확성) | 0 | 0 | 빠른 플릭에서 다른 사진이 잠깐 보이면 실패 |
| 메모리 상한 | < 400MB | < 400MB | 장시간 스크롤/뷰어 왕복 후 (Allocations) |
| 삭제 반영 | < 500ms | < 700ms | UI에서 사라지고 상태 안정화 시점 |
| 콜드 스타트 | < 2s | < 2.5s | 첫 화면 인터랙션 가능 시점 |

**지표 정의 보충:**
- **hitch 비율**: 스크롤 중 프레임 예산(120Hz: 8.3ms / 60Hz: 16.7ms)을 초과한 프레임의 비율. 사용자가 "끊김"으로 체감. Instruments Core Animation으로 계측하며, 산출 방식(샘플링 구간/스크롤 시나리오/집계)은 스파이크에서 확정.
- **첫 그리드 유효 썸네일**: 최초 화면에서 placeholder가 실 이미지로 대체되어 "그리드가 채워졌다"고 느껴지는 시점. 앱 진입부터 최초 화면 셀 N개(예: 12개)가 실 이미지로 채워질 때까지의 시간. N은 스파이크에서 확정.
- **오표시**: 셀 재사용 시 이전 이미지가 잠깐 보이는 현상. 0이 아니면 품질 실패.
- **메모리 상한**: 이 수치를 초과하면 시스템 메모리 경고/앱 종료 위험 증가.

---

## 3. 비목표 (Non-Goals, MVP 기준)

| 항목 | 상태 | 비고 |
|------|------|------|
| iCloud 사진 보관함 | 제외 | `isNetworkAccessAllowed = false` 정책 |
| 의미 검색 (사람/장소/사물) | 제외 | 장기 옵션, 별도 로드맵 |
| 앱 내 휴지통/복구 | MVP 제외 | 후속 버전 (M4) |
| 앱 내 Undo 토스트 | 제외 | 시스템 Photos '최근 삭제됨'에서 복구 |
| 즐겨찾기 (하트) | 제외 | |
| Years/Months/Days 모드 | MVP 제외 | M2~M3에서 확장 |

---

## 4. 타깃/전제

### 4.1 대용량 기준

- 성능 보장 기준: **5만 장** 라이브러리에서도 성능 저하 없이 동작
- 원칙: "전체 에셋 배열 생성/유지" 및 "전체 스냅샷 반복 생성"을 설계적으로 피함

### 4.2 데이터 범위 (로컬 중심)

- 로컬(기기) 사진 중심 최적화
- iCloud-only 에셋: MVP에서는 "다운로드 시도 안함" 정책
  - 사용자에게 명확한 상태 표시 (placeholder/안내)
  - 세부 UX는 스파이크에서 확정

---

## 5. MVP 범위 (Scope)

### 5.1 화면

| 화면 | 설명 |
|------|------|
| 라이브러리 | All Photos 그리드 |
| 앨범 탭 | 사용자 앨범 리스트 + 앨범 진입 (앨범 내 그리드) |
| 스마트 앨범 | Screenshots (필수), Videos/Selfies (선택) |
| 뷰어 | 전체화면 사진 뷰어 (그리드에서 진입) |

### 5.2 공통 상호작용

**핀치 연속 줌**
- MVP: All Photos 내부에서 밀도 변화 제공
- 목표 동작: Photos처럼 부드럽게 변화하는 감성
- Years/Months/Days 모드 스냅은 MVP 이후

**앵커 유지 (핵심)**
- 핀치 제스처 중심점(두 손가락 중간) 아래 콘텐츠를 앵커로 캡처
- 줌 변화 중/후에도 해당 콘텐츠가 튀지 않게 유지 (오프셋 보정)

**멀티 선택**
- Select 모드로 여러 장 선택 후 일괄 삭제

### 5.3 삭제 정책

| 항목 | 정책 |
|------|------|
| 삭제 정책 | 항상 "사진 삭제"로 단순화 (앨범에서 "앨범 제거" 없음) |
| 확인 팝업 | 없음 (즉시 삭제) |
| 삭제 위치 | 그리드 (단일 + Select 모드) + 뷰어 (단일) |
| 뷰어 삭제 제스처 | **위 스와이프 = 삭제** |
| 삭제 후 이동 (뷰어) | **이전 사진** → 없으면 다음 → 없으면 그리드 복귀 |
| 복구 | 시스템 Photos '최근 삭제됨'에서 (MVP) |
| 앱 내 휴지통 | 후속 버전 (M4) |

---

## 6. 핵심 설계 원칙 (요구사항 관점)

> 본 섹션은 "무엇을 달성해야 하는지"를 정의합니다.
> 구체적인 구현 방식은 [tech_spec2.md](./tech_spec2.md)에서 다룹니다.

### 6.1 대용량 (5만 장) 원칙

- "전체 에셋 배열 생성/유지" 및 "전체 스냅샷 반복 생성"을 설계적으로 피함
- 안정적인 식별자 기반으로만 이동/선택/앵커 유지

### 6.2 스크롤 품질 원칙

- 보이는 것만 요청 + 선제 캐싱 + 요청 취소 강제
- 그리드: 작은 타깃 사이즈로 디코딩/메모리 절약
- 뷰어: 단계적 품질 향상 (저→고)

### 6.3 UI 성능 원칙

- 핵심 스크롤 영역 (그리드/뷰어): 성능 최적화된 UI 기술 사용
- 비핵심 영역 (설정/셸): 생산성 중심 기술 사용 가능

### 6.4 ProMotion 지원 원칙

- iPhone 17 Pro 등 120Hz 디스플레이에서 120fps 체감 제공
- 메인 스레드 작업 최소화 (120Hz에서 프레임 버짓 8.3ms)
- 이미지 디코딩 비동기화

---

## 7. 기술 방향 (권장, 스파이크에서 검증)

> 아래는 PRD 목표 달성을 위한 **권장 방향**입니다.
> 스파이크 결과에 따라 변경될 수 있으며, 최종 결정은 Tech Spec에서 확정합니다.

### 7.1 그리드 UI 기술

| 기준 | SwiftUI LazyVGrid | UIKit UICollectionView |
|------|-------------------|------------------------|
| 셀 재사용 | 제한적 | 완전한 재사용 풀 |
| 대용량 메모리 | 누적 증가 위험 | 일정 수준 유지 |
| 프리패치 제어 | 제한적 | 완전 지원 |
| 120Hz 제어 | 제한적 | 완전 제어 |

**권장 방향**: UIKit 기반 (스파이크 S1에서 검증)

### 7.2 이미지 캐싱

- PhotoKit 공식 API (`PHCachingImageManager`) 기반 권장
- 가시 영역 기반 프리히트 정책 필요
- 세부 정책은 스파이크 S2에서 확정

### 7.3 이미지 로딩 정책

- 그리드: 작은 타깃 사이즈 + 빠른 포맷 우선 (스크롤 품질 우선)
- 뷰어: 단계적 품질 향상 허용 (전환/제스처 중 프레임 예산 준수)

---

## 8. 아키텍처 경계 (모듈 정의)

후속 기능(Years/Months/Days, 빠른 이동, 검색)을 추가해도 코어를 흔들지 않기 위한 고정 규칙.

| 모듈 | 책임 |
|------|------|
| **LibraryStore** | PhotoKit fetch/권한/변경 감지, 정렬/필터 정책 단일 소스 |
| **AlbumStore** | 사용자 앨범/스마트 앨범 관리 |
| **TimelineIndex** | 줌 레벨별 섹션/버킷 경계, ID↔indexPath 매핑, 앵커 점프 (MVP: All-only) |
| **ImagePipeline** | 요청/취소/캐시 정책 단일화, preheat 포함 |
| **GridController** | UI 어댑터 (프리패치, 재사용, 멀티선택), 이미지는 ImagePipeline에만 위임 |
| **ViewerCoordinator** | 뷰어 내비게이션, 삭제 후 이동, 전환 정책 |
| **DeletionService** | 단일/멀티 삭제 처리 |
| **SearchService** | (MVP 제외) 쿼리 → assetID[] 인터페이스만 고정 |

---

## 9. 스파이크 계획 (구현 전 검증)

목표: 핵심 리스크를 짧은 실험으로 판정하고, 결과로 KPI를 현실적으로 보정.

| ID | 주제 | 검증 내용 |
|----|------|-----------|
| S1 | 대용량 데이터 소스 | 5만 장에서 "전체 배열/스냅샷" 없이 그리드 안정 동작 검증 |
| S2 | 프리히트 정책 | ±N 화면 프리캐시, 속도 적응형 윈도우의 hitch/메모리 트레이드오프 |
| S3 | 요청 취소/정확성 | 셀 재사용/빠른 플릭에서 오표시 0 보장 패턴 확정 |
| S4 | 핀치 앵커 보정 | 핀치 중심 앵커 유지 (점프/떨림/지연) 검증, 폴백 규칙 확정 |
| S5 | 삭제/변경 감지 | 멀티삭제/연속삭제 후 그리드/뷰어 상태 안정성 검증 |
| S6 | 120fps 달성 | ProMotion 디바이스에서 스크롤/핀치/전환 중 hitch 비율이 목표치 이내인지 계측하고, 병목(메인 스레드/디코딩/레이아웃)을 분류해 개선 포인트 도출 |

---

## 10. 계측/검증 (Definition of Done)

### 10.1 계측 도구

- **Instruments**: Time Profiler, Core Animation (hitch), Allocations/Leaks
- **MetricKit**: 앱 성능 메트릭 수집 (릴리즈 품질 확인)
- **XCTest**: 자동화된 성능 테스트

### 10.2 수용 기준 (MVP Gate)

MVP는 다음 조건을 **모두** 만족해야 합니다:

| 기준 | 조건 |
|------|------|
| 스크롤 품질 | 5만 장에서 hitch 비율이 목표치 이내 |
| 정확성 | 빠른 플릭/역방향 스크롤에서 오표시 0 |
| 핀치 줌 | 앵커가 튀지 않고, 입력 지연 체감 없음 |
| 삭제 안정성 | 멀티/연속 삭제 후 UI 안정 수렴 |
| 메모리 | 장시간 사용 후 400MB 이내 유지 |

---

## 11. 마일스톤 (로드맵)

### M0. 명세 고정 (현재)

- 본 PRD 기반 "불변 규칙" 요약본 작성
- 스파이크 계획 확정

### M1. MVP

| 기능 | 상세 |
|------|------|
| All Photos | 그리드 (핀치 줌/앵커 유지) |
| 앨범 | 사용자 앨범 리스트 + 앨범 진입 그리드 |
| 스마트 앨범 | Screenshots 등 일부 노출 |
| 뷰어 | 전체화면, 좌/우 스와이프 탐색 |
| 삭제 | 위 스와이프 (뷰어) + Select 모드 (그리드), 확인 없이 즉시 |
| 복구 | 시스템 Photos '최근 삭제됨'에서 |

### M2. Zoom 확장

- Days 추가 (All ↔ Days 스냅)

### M3. Full Timeline

- Months/Years 추가 + 4단 스냅 완성
- 빠른 이동 (스크럽/인덱스) UI 추가

### M4. 앱 내 휴지통

- 앱 내부 휴지통/복구 기능

### M5. 검색 (옵션)

- 의미 검색 (사람/장소/사물)
- 별도 프로젝트로 착수/보류 결정

---

## 12. 문서 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|-----------|
| 4.0 | 2025-12-15 | PRD1 + PRD2 병합, 기술 설계는 tech_spec2.md로 분리 |
| 4.1 | 2025-12-15 | 배경 추가, 디바이스별 성능 목표 분리, 계측/검증 섹션 추가, 구현 방식→요구사항 관점으로 수정 |
| 4.2 | 2025-12-15 | KPI 측정 정의 상세화 (프레임 예산, 셀 개수), S6 스파이크 설명 보강 |
