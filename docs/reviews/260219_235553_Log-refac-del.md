**핵심 발견사항 (심각도 순)**  
1. **[높음] 삭제 대상 누락 1건**  
`Sources/AppCore/Services/Log.swift:180`에 `SystemUIInspector`가 `false`인데, 실제 호출이 `PickPhoto/PickPhoto/Debug/SystemUIInspector.swift:60`에 있습니다.  
문서 Phase 목록(`docs/260217Log-refac-del.md:213` 이후)에는 이 파일이 없습니다.  
이 상태면 “false 카테고리 로그 전량 삭제” 목표가 깨집니다.

2. **[높음] R1~R6로는 분기 케이스 일부 미커버**  
R3는 “if 블록 단독 로그”만 다룹니다(`docs/260217Log-refac-del.md:77`).  
하지만 실제로는 `switch case`에서 로그만 있는 케이스가 있습니다: `PickPhoto/PickPhoto/Debug/AutoScrollTester.swift:200`.  
이런 케이스는 로그 삭제 후 `case`가 비어 컴파일 이슈가 날 수 있어, 별도 규칙(R7)이 필요합니다.

3. **[높음] 검증 단계가 미등록 카테고리 누락을 잡지 못함**  
문서는 미등록 카테고리 문제를 인지했지만(`docs/260217Log-refac-del.md:15`), 검증은 “false 카테고리명 grep” 중심입니다(`docs/260217Log-refac-del.md:256`).  
`PreviewService` 같은 미등록 호출(`PickPhoto/PickPhoto/Features/AutoCleanup/Preview/CleanupPreviewService.swift:87`)은 이 방식으로 빠질 수 있습니다.

4. **[중간] Phase 수치/파일 수 표기가 내부적으로 불일치**  
- Phase 3 헤더 `~53` vs 표 합계(24+13+10+9+6=62): `docs/260217Log-refac-del.md:145`, `docs/260217Log-refac-del.md:149`  
- Phase 5 헤더 `~75` vs 표 합계(80): `docs/260217Log-refac-del.md:193`, `docs/260217Log-refac-del.md:197`  
- Phase 6 헤더 “15파일”인데 표는 19파일: `docs/260217Log-refac-del.md:213`, `docs/260217Log-refac-del.md:217`  
진행률/완료 판정이 흔들릴 수 있습니다.

5. **[중간] R4(로그 전용 변수 정리) 범위가 좁음**  
문서의 R4 대상 3파일 외에도 로그 전용 타이밍 변수가 있습니다. 예: `PickPhoto/PickPhoto/Features/Grid/GridDataSourceDriver.swift:338`와 `PickPhoto/PickPhoto/Features/Grid/GridDataSourceDriver.swift:355`.  
컴파일 에러는 아닐 수 있어도 경고/잔여 dead code가 남습니다.

---

**질문하신 포인트별 결론**  
- **계획이 목표 달성에 충분한가?**: 현재 상태는 **거의 충분하지만, 위 1~3번 보완 없이는 불충분**합니다.  
- **R1~R6 커버리지**: **불완전**합니다. `switch case 로그-only` 처리를 추가해야 합니다.  
- **Phase 분류 적절성**: 도메인 분류 자체는 좋지만, **수치/파일 불일치 정정이 필요**합니다.  
- **위험 패턴 처리**: 핵심은 잡았지만, **분기 패턴(R7) + 검증 강화**가 더 필요합니다.

---

**문서에 바로 추가 권장**  
1. `R7`: `switch case` 내 로그-only 케이스 처리 규칙 추가.  
2. Phase 6에 `SystemUIInspector.swift` 추가.  
3. 검증에 “미등록 카테고리 0건” 체크 추가.  
4. 각 Phase 헤더 수치와 표 합계를 일치시켜 재산정.  
5. R4를 “로그 전용 변수/카운터/헬퍼 함수 정리”로 일반화.