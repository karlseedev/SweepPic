결론부터 말하면, 현재 `docs/db/260212db-Impl.md`는 **그대로 구현에 들어가기엔 부족합니다**. 특히 이벤트 2/5/7에서 집계 정확도와 구현 가능성 이슈가 있습니다.

1. [Critical] 권한 이벤트(이벤트 2) `firstRequest`/`settingsChange` 분리 논리가 실제 코드 흐름과 충돌합니다.  
`docs/db/260212db-Impl.md:206`, `docs/db/260212db-Impl.md:483`은 경로 분리만으로 timing을 구분한다고 하지만, 실제로 `PermissionStore`는 `requestAuthorization()`와 `checkAndNotifyIfChanged()` 둘 다 동일 `notifyChange`를 타고(`Sources/AppCore/Stores/PermissionStore.swift:90`, `Sources/AppCore/Stores/PermissionStore.swift:109`), `SceneDelegate`는 같은 핸들러에서 처리합니다(`PickPhoto/PickPhoto/App/SceneDelegate.swift:167`).  
현재 계획대로면 첫 권한 요청에도 `settingsChange`가 중복 집계될 가능성이 큽니다.

2. [Critical] 유사 그룹 종료(이벤트 5-2) “삭제 없이 닫기” 매핑이 잘못되었습니다.  
계획은 `ViewerViewController+SimilarPhoto.didDeletePhotos`를 “삭제 없이 닫기”로 사용한다고 적었는데(`docs/db/260212db-Impl.md:156`), 이 콜백은 삭제 시에만 호출됩니다(`PickPhoto/PickPhoto/Features/Viewer/ViewerViewController+SimilarPhoto.swift:743`).  
삭제 없이 닫기는 `faceComparisonViewControllerDidClose` 경로입니다(`PickPhoto/PickPhoto/Features/Viewer/ViewerViewController+SimilarPhoto.swift:758`, `PickPhoto/PickPhoto/Features/SimilarPhoto/UI/FaceComparisonViewController.swift:499`).

3. [High] `coordinator.deleteSource` 사용 계획은 컴파일 관점에서 미완성입니다.  
계획은 `ViewerViewController`에서 `coordinator.deleteSource`를 직접 사용합니다(`docs/db/260212db-Impl.md:501`).  
하지만 `coordinator` 타입은 `ViewerCoordinatorProtocol`이며(`PickPhoto/PickPhoto/Features/Viewer/ViewerViewController.swift:94`), 해당 프로토콜에 source 프로퍼티가 없습니다(`PickPhoto/PickPhoto/Features/Viewer/ViewerCoordinator.swift:19`).  
또한 아키텍처는 `source: DeleteSource`(non-optional) 전달을 권장하는데(`docs/db/260212db-Archi.md:530`), 구현계획은 optional로 풀어 silent 누락 위험이 있습니다(`docs/db/260212db-Impl.md:138`).

4. [High] 유사 분석 duration 수집 지점(이벤트 5-1)이 현재 함수 시그니처와 맞지 않습니다.  
계획은 `postAnalysisComplete`에서 `elapsed`를 넘기려 하지만(`docs/db/260212db-Impl.md:521`), 해당 함수는 duration을 받지 않습니다(`PickPhoto/PickPhoto/Features/SimilarPhoto/Analysis/SimilarityAnalysisQueue.swift:1211`).  
duration은 `formGroupsForRange` 내부 `totalTime`에서만 계산됩니다(`PickPhoto/PickPhoto/Features/SimilarPhoto/Analysis/SimilarityAnalysisQueue.swift:391`, `PickPhoto/PickPhoto/Features/SimilarPhoto/Analysis/SimilarityAnalysisQueue.swift:437`).

5. [High] 이벤트 7(정리) 호출 지점 표가 현재 코드 상태와 어긋나 있습니다.  
계획은 `GridViewController+Cleanup`의 기존 플로우 라인들을 기준으로 잡았지만(`docs/db/260212db-Impl.md:166`), 현재 파일은 테스트 코드가 활성화되어 있고 실제 플로우는 주석 처리되어 있습니다(`PickPhoto/PickPhoto/Features/Grid/GridViewController+Cleanup.swift:125`, `PickPhoto/PickPhoto/Features/Grid/GridViewController+Cleanup.swift:133`).  
이 상태로는 7-1/7-2 트래커 설계가 바로 적용되지 않습니다.

6. [Medium] 권한 결과 enum 매핑 규칙이 문서에 명시되지 않았습니다.  
아키텍처 enum은 `fullAccess/limitedAccess/denied` 3개만 정의(`docs/db/260212db-Archi.md:638`)인데, 실제 앱 권한 상태에는 `restricted`가 존재합니다(`Sources/AppCore/Models/PermissionState.swift:23`, `Sources/AppCore/Services/PhotoLibraryService.swift:138`).  
`restricted`를 어떤 값으로 보낼지 명시가 필요합니다.

7. [Medium] 아키텍처의 SDK 세션 설명(“5분 타임아웃”)은 최신 공식 README와 불일치 가능성이 있습니다.  
아키텍처는 5분 기준을 전제로 하고(`docs/db/260212db-Archi.md:91`), GitHub README는 백그라운드 복귀 시 세션 ID 갱신으로 설명합니다(웹 소스 참조).  
이 부분은 구현 전 재검증이 필요합니다(설계 근거 이슈).

아키텍처 일관성 평가는 다음과 같습니다.
- 일치하는 부분: 파일 분리/확장 구조, SceneDelegate 훅 위치, AppCore 브릿지 방향은 아키텍처와 대체로 일치합니다.
- 불일치하는 부분: source 전달 설계(타입/옵셔널성), 이벤트 5-2 경로 정의, 이벤트 7 실제 코드 기준점.

웹 교차검증(2026-02-13 확인):
- TelemetryDeck iOS setup 가이드는 여전히 SwiftSDK `from: "2.11.0"` 기준 예시를 사용합니다.  
  https://telemetrydeck.com/docs/articles/ios-sdk-setup/
- Swift Package Index도 SwiftSDK `2.11.0`을 표시합니다.  
  https://swiftpackageindex.com/TelemetryDeck/SwiftSDK
- GitHub README의 세션 설명은 아키텍처 문서의 “5분 기준”과 표현이 다릅니다.  
  https://github.com/TelemetryDeck/SwiftSDK

권장 수정 순서:
1. 이벤트 2 timing 분기부터 재설계(중복 집계 차단).  
2. 이벤트 5-2 종료 경로를 `didDeletePhotos`와 `didClose`로 분리 정의.  
3. `ViewerCoordinatorProtocol`/source 모델을 먼저 확정(컴파일 가능 상태로).  
4. 이벤트 5-1 duration 전달 경로를 `formGroupsForRange` 기준으로 재작성.  
5. 이벤트 7은 현재 테스트 코드 복원 여부를 먼저 결정한 뒤 라인 기준 재작성.