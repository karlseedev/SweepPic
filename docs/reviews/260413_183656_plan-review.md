**Findings**
1. High: `path1WeakLoose`를 “Precision 모드에서 어차피 항상 false”로 보고 삭제하는 부분은 현재 코드 기준으로 성립하지 않습니다. 계획서는 [glistening-enchanting-llama.md](/Users/karl/.claude/plans/glistening-enchanting-llama.md:132)에 그렇게 적혀 있지만, 실제 `precision`은 Strong 신호만 저품질로 보며([JudgmentMode.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/AutoCleanup/Models/JudgmentMode.swift:24), [QualityAnalyzer.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/AutoCleanup/Analysis/QualityAnalyzer.swift:582)), `path1WeakLoose`는 바로 그 “저품질은 아니지만 weak 신호 합산이 있는 사진”을 `.deep`으로 끌어올리는 로직입니다([CleanupPreviewService.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/AutoCleanup/Preview/CleanupPreviewService.swift:360)). 이걸 그대로 삭제하면 기존 deep-only 후보 일부가 2단계 어디에도 안 들어가게 됩니다. 특히 `aestheticsScore == nil`도 허용하고 있어 iOS 16~17 영향도 있습니다. 이건 “3단계→2단계 축소”를 넘어서 탐지 범위를 줄이는 변경이라, `standard`로 흡수할지 의사결정이 먼저 필요합니다.

2. Medium: 계획의 수정 파일 목록에 `Debug/PhotoCell+CompareBadge.swift`가 빠져 있습니다. Phase 5에서 `ModeCategory.deepOnly`를 삭제한다고 했지만([glistening-enchanting-llama.md](/Users/karl/.claude/plans/glistening-enchanting-llama.md:181)), 실제로 `setModeBadge`가 `.deepOnly`를 switch하고 있어([PhotoCell+CompareBadge.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Debug/PhotoCell+CompareBadge.swift:82)) DEBUG 빌드가 깨집니다. 현재 9개 파일로는 부족합니다.

3. Medium: 계획서에 `ModeComparisonSummary.deepOnlyCount` 삭제라고 되어 있는데([glistening-enchanting-llama.md](/Users/karl/.claude/plans/glistening-enchanting-llama.md:188)), 실제 코드에는 `ModeComparisonSummary`가 없고 `ModeComparisonResult.deepOnlyCount`입니다([ModeComparisonTester.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Debug/ModeComparisonTester.swift:48), [ModeComparisonTester.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Debug/ModeComparisonTester.swift:419)). 문서가 실제 심볼과 어긋나 있어서 구현자가 그대로 따라가면 수정 지점을 놓칠 가능성이 있습니다.

4. Medium: `PreviewMaxStage.deep` 제거는 코드 내부 수정만이 아니라 분석 이벤트 스키마 변경입니다. 계획은 enum 삭제만 적고 끝나지만([glistening-enchanting-llama.md](/Users/karl/.claude/plans/glistening-enchanting-llama.md:175)), 실제 전송 파라미터는 `maxStageReached.rawValue`이고([AnalyticsService+Cleanup.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Shared/Analytics/AnalyticsService+Cleanup.swift:46)), 문서에는 아직 `light, standard, deep`로 정의돼 있습니다([260225db-Archi.md](/Users/karl/Project/Photos/iOS/docs/db/260225db-Archi.md:757)). 대시보드/검증 스크립트가 이 값을 전제로 한다면 문서와 테스트 케이스도 같이 바꿔야 합니다.

5. Low: 검증 계획이 컴파일과 기본 UI 시나리오에 치우쳐 있습니다([glistening-enchanting-llama.md](/Users/karl/.claude/plans/glistening-enchanting-llama.md:238)). 이번 변경의 핵심은 enum 삭제보다 분류 경계 이동이므로, `-0.2`와 `0.2` 경계 샘플, `path1WeakLoose` 처리방식, DEBUG 빌드까지 명시적으로 확인해야 충분합니다.

**Open Questions**
- 기존 `.deep` 중 `path1WeakLoose`로만 잡히던 사진은 2단계(`standard`)로 편입할 건지, 이번 변경에서 아예 버릴 건지 결정이 필요합니다.
- `cleanup.previewCompleted.maxStageReached`에서 `deep` 값을 완전히 제거해도 되는지, 아니면 분석 시스템 호환성 때문에 enum 값만 남겨둘지 확인이 필요합니다.

**Summary**
핵심 Preview 폴더 중심의 방향은 맞습니다. 다만 현재 계획은 “UI 3단계→2단계 축소”에는 근접해 있어도, 실제 분류 로직 보존과 DEBUG/analytics 파급까지 포함하면 아직 충분하지 않습니다. 최소한 `path1WeakLoose` 처리 정책, `PhotoCell+CompareBadge.swift` 추가, 잘못 적힌 `ModeComparisonSummary` 수정, analytics 문서/검증 범위 보강은 계획서에 반영하는 게 맞습니다.