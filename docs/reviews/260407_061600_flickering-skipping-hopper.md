검토 결과: 방향은 맞지만, 계획 그대로 구현하면 C 사전분석이 “그룹을 찾았는데도 0건 완료로 기록”되는 race와, 뷰어 +버튼이 안 뜨는 얼굴 캐시 문제가 생길 수 있습니다. 웹검색은 하지 않았습니다. 이 건은 외부 최신 정보보다 현재 코드의 제어 흐름과 캐시 계약 확인이 핵심이었습니다.

**주요 피드백**
- **High: 그룹 발견 후 0건으로 오인될 race가 있습니다.** 계획은 `analyzeForFirstGroup()`에서 “콜백 호출 + return”으로 끝내고 기존 `onGroupFound` 로직을 유지하라고 되어 있습니다. 그런데 현재 호출부는 `onGroupFound` 안에서 별도 `Task`로 `SimilarityCache` 브리지와 `UserDefaults` 저장을 비동기 실행하고, `analyze()` 완료 후에는 `Self.cPreScanFoundAssetID == nil`이면 0건 완료로 기록합니다. 현재는 `service.cancel()` → `CancellationError` 경로로 자연 완료 블록을 피하기 때문에 버티고 있습니다. 새 메서드가 정상 return하면 저장 Task보다 자연 완료 블록이 먼저 실행될 수 있습니다.  
  근거: [/Users/karl/.claude/plans/flickering-skipping-hopper.md](/Users/karl/.claude/plans/flickering-skipping-hopper.md):85, [/Users/karl/.claude/plans/flickering-skipping-hopper.md](/Users/karl/.claude/plans/flickering-skipping-hopper.md):100, [/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/Grid/GridViewController+CoachMarkC.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/Grid/GridViewController+CoachMarkC.swift):357, [/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/Grid/GridViewController+CoachMarkC.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/Grid/GridViewController+CoachMarkC.swift):394.  
  권장: `analyzeForFirstGroup()`을 `async throws -> FaceScanGroup?`로 만들거나, 최소한 호출부에 동기적인 `didFindGroup` 플래그를 두고 0건 완료 판정을 `didFindGroup == false`일 때만 하세요. 가능하면 브리지/저장까지 순차 `await`되는 구조가 더 안전합니다.

- **High: 얼굴 캐시에 `isValidSlot`이 반영되어야 합니다.** 계획의 “얼굴 데이터를 FaceScanCache에 저장”은 너무 모호합니다. `CachedFace.isValidSlot` 기본값은 `false`이고, 뷰어는 `SimilarityCache.shared.getValidSlotFaces()`가 비어 있으면 +버튼을 숨깁니다. 새 메서드가 raw `photoFacesMap`을 그대로 `FaceScanCache`에 넣고 기존 브리지를 유지하면, C-1은 얼굴 존재만 보고 통과할 수 있어도 C-2/뷰어 +버튼은 안 뜰 수 있습니다.  
  근거: [/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/SimilarPhoto/Models/CachedFace.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/SimilarPhoto/Models/CachedFace.swift):61, [/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/Viewer/ViewerViewController+SimilarPhoto.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/Viewer/ViewerViewController+SimilarPhoto.swift):557, [/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/FaceScan/Service/FaceScanService.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/FaceScan/Service/FaceScanService.swift):276.  
  권장: 기존 `analyze()`처럼 `isolatedCache.addGroupIfValid(... photoFaces: photoFacesMap)`를 거친 뒤 `isolatedCache.getFaces()`로 `isValidSlot`이 갱신된 얼굴을 `FaceScanCache`에 복사하세요. 아니면 수동으로 `validSlots.contains(face.personIndex)`를 반영한 얼굴 배열을 저장해야 합니다.

- **Medium: 검색 순서가 기존 `.fromLatest` 의미와 달라질 수 있습니다.** `fetchAllPhotos()`는 오래된 것부터 정렬되고, 기존 `analyze()`는 raw group을 `reversed()`로 소비해 최신 그룹부터 전달합니다. 계획대로 `findPhotoBasedLower` → `fetchPhotosInRange` → 앞에서부터 100장 체크를 하면 “최신 2,000장 중 가장 오래된 쪽”부터 찾게 됩니다. C에서 아무 그룹이나 괜찮다면 문서에 명시하고, 최신 우선이 목표라면 `photos` 처리 순서를 뒤집거나 체크포인트 소비 순서를 조정해야 합니다.  
  근거: [/Users/karl/Project/Photos/iOS/Sources/AppCore/Services/PhotoLibraryService.swift](/Users/karl/Project/Photos/iOS/Sources/AppCore/Services/PhotoLibraryService.swift):92, [/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/FaceScan/Service/FaceScanService.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/FaceScan/Service/FaceScanService.swift):244.

- **Medium: 취소는 “즉시 취소”가 아닙니다.** `FaceScanService.cancel()`은 서비스 플래그만 세우고, `PersonMatchingEngine.generateFeaturePrints()` 내부는 `Task` 취소를 봅니다. 계획의 “매 배치마다 cancelled 확인”만으로는 현재 실행 중인 20장 FP 생성이나 `assignPersonIndicesForGroup()` 도중 즉시 중단되지 않습니다. 사전분석 리셋/화면 이탈 시 빠른 중단이 필요하면 호출부가 `scanTask`를 보관해 `Task.cancel()`까지 해야 합니다.  
  근거: [/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/FaceScan/Service/FaceScanService.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/FaceScan/Service/FaceScanService.swift):107, [/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/SimilarPhoto/Analysis/PersonMatchingEngine.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/SimilarPhoto/Analysis/PersonMatchingEngine.swift):111.

- **Medium/Low: 전용 로더는 `.shared.pause()`로부터만 독립입니다.** `SimilarityImageLoader()` 주입 자체는 타당합니다. `waitIfPaused()`와 `pauseCount`가 인스턴스 상태라서 `.shared.pause()` 영향은 피합니다. 다만 새 로더도 `PHCachingImageManager`/photosd/iCloud 다운로드 자원은 공유하므로 스크롤 중 성능 경합은 여전히 측정해야 합니다. 특히 로더 옵션이 `isNetworkAccessAllowed = true`라 앱 시작 사전분석 2,000장 정책은 iCloud 다운로드/배터리 리스크를 문서에 명시하는 편이 좋습니다.  
  근거: [/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/SimilarPhoto/Analysis/SimilarityImageLoader.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/SimilarPhoto/Analysis/SimilarityImageLoader.swift):126, [/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/SimilarPhoto/Analysis/SimilarityImageLoader.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/SimilarPhoto/Analysis/SimilarityImageLoader.swift):411.

충분한 부분도 있습니다. `PersonMatchingEngine`은 이미 `imageLoader` 주입을 지원하므로 `FaceScanService`의 `matchingEngine`을 init 주입으로 바꾸는 계획은 정합성이 있습니다. 기존 `FaceScanService(cache:)` 호출부도 기본값 `.shared`를 두면 유지됩니다. 다만 위 두 High 항목은 구현 전에 계획에 반영하는 것을 권장합니다.