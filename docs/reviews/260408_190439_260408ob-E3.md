검토 결과: 방향은 맞지만, 그대로 구현하기에는 몇 군데가 부족합니다. 웹검색은 필요하지 않았고, 로컬 코드 기준으로 확인했습니다.

**주요 피드백**
- [High] E-3용 하이라이트 구멍이 현재 계획만으로는 생기지 않습니다. 새 타입 `.trashRestore`를 추가해도 `updateDimPath()`는 `.gridSwipeDelete`, `.similarPhoto`, `.faceComparisonGuide`, D/E 일부 타입만 구멍을 뚫습니다. E-3를 별도 타입으로 쓸 거면 `.trashRestore` rect branch를 반드시 추가해야 합니다.  
  [CoachMarkOverlayView.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Shared/Components/CoachMarkOverlayView.swift):359  
  [260408ob-E3.md](/Users/karl/Project/Photos/iOS/docs/onboarding/260408ob-E3.md):34

- [High] `assetID` 전달 계획은 큰 줄기는 맞지만, 폴백 경로가 위험합니다. 문서의 `findTrashCell(assetID:) -> (cell, frame, snapshot)`는 “첫 번째 visible 셀 폴백”을 하면서도 실제 복구할 `resolvedAssetID`를 반환하지 않습니다. 이러면 폴백 셀을 보여주고 원래 `assetID`를 복구하거나, `assetID == nil`이면 아무 것도 복구하지 않는 빈틈이 생깁니다. 반환값에 `resolvedAssetID`를 포함해야 합니다.  
  [260408ob-E3.md](/Users/karl/Project/Photos/iOS/docs/onboarding/260408ob-E3.md):241  
  [260408ob-E3.md](/Users/karl/Project/Photos/iOS/docs/onboarding/260408ob-E3.md):260

- [High] “E-1+E-2 동안 TrashStore 변경 알림으로 TrashAlbumViewController fetchResult 갱신이 보장된다”는 근거가 정확하지 않습니다. `TrashStore.onStateChange`는 단일 `changeHandler`를 덮어쓰는 구조라 다중 관찰자가 아닙니다. 실제로는 E-1+E-2가 삭제대기함 탭으로 전환하면서 `TrashAlbumViewController.viewWillAppear`의 `loadTrashedAssets()`가 다시 도는 흐름에 기대야 합니다. 이 로드는 백그라운드 fetch라 일반적으로 충분하지만 절대 보장은 아닙니다. E-3 트리거 시 셀을 못 찾으면 즉시 스킵하지 말고 짧은 재시도 또는 `targetAssetID` 기반 no-animation restore fallback이 필요합니다.  
  [TrashStore.swift](/Users/karl/Project/Photos/iOS/Sources/AppCore/Stores/TrashStore.swift):307  
  [TrashAlbumViewController.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/Albums/TrashAlbumViewController.swift):211  
  [TrashAlbumViewController.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/Albums/TrashAlbumViewController.swift):363  
  [260408ob-E3.md](/Users/karl/Project/Photos/iOS/docs/onboarding/260408ob-E3.md):253

- [Medium] 복구 후 UI 갱신 순서는 개념적으로 자연스럽지만, 카드 표시 전 `highlightFrame = .zero` 후 `updateDimPath()`로 구멍을 닫는 단계가 문서에 빠져 있습니다. 복구 후 컬렉션뷰가 reload되면 셀들이 이동할 수 있는데, 기존 하이라이트 구멍이 남아 있으면 스냅샷 페이드아웃 뒤에 엉뚱한 셀이 구멍으로 보일 수 있습니다.  
  [260408ob-E3.md](/Users/karl/Project/Photos/iOS/docs/onboarding/260408ob-E3.md):263  
  [TrashAlbumViewController.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Features/Albums/TrashAlbumViewController.swift):423

- [Medium] 새 `CoachMarkOverlayView+E3.swift`에서 replay 코드를 그대로 복사하면 접근 제어 문제가 납니다. `swipeRatio`, `swipeDistance`, `performSingleDeleteSwipe()`는 현재 `private`라 별도 파일 extension에서 접근할 수 없습니다. E-3는 로컬 `let swipeDistance = highlightFrame.width`를 쓰거나 별도 associated property를 두는 식으로 계획에 명시하는 게 안전합니다.  
  [CoachMarkOverlayView.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Shared/Components/CoachMarkOverlayView.swift):181  
  [CoachMarkOverlayView.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Shared/Components/CoachMarkOverlayView.swift):229  
  [CoachMarkOverlayView.swift](/Users/karl/Project/Photos/iOS/SweepPic/SweepPic/Shared/Components/CoachMarkOverlayView.swift):601

**질문별 판단**
1. E-1+E-2 dismiss 후 0.3초 트리거는 “이전 오버레이 정리” 기준으로는 대체로 안전합니다. 다만 “삭제대기함 셀 준비 완료” 기준으로는 안전 보장이 부족합니다. 재시도 또는 로드 완료 확인을 추가하세요.
2. `assetID` 전달 경로는 첫 사용과 다시보기 모두 캡처 지점은 맞습니다. 하지만 셀 탐색 폴백에서 `resolvedAssetID`가 빠진 것이 핵심 빈틈입니다.
3. 실제 복구 후 UI 갱신은 스냅샷으로 가리는 접근이 맞습니다. 단, 복구 성공 확인 후 카드 표시, 하이라이트 구멍 제거, 실패 시 처리 정책이 더 필요합니다.
4. A replay/D-1 패턴을 섞는 방향은 일관됩니다. 다만 `.trashRestore` 타입을 추가하는 만큼 `updateDimPath`, cleanup, UserDefaults reset/skip, 접근 제어까지 같이 맞춰야 구현 품질이 맞습니다.