# 회전 시 레이아웃 깨짐 수정 계획

## 문제 요약

디바이스 회전 시 **뷰어 + 그리드** 화면에서 레이아웃이 깨지는 문제.

### 근본 원인

**PhotoPageViewController.swift**:
- `hasAppliedInitialLayout = true`로 고정되어 회전 후 `applyInitialLayout()` 재호출 안 됨
- `viewDidLayoutSubviews()`에서 `scrollView.frame`만 업데이트
- `imageView.frame`, `contentSize`, `contentInset` 회전 후 재계산 안 됨

**ViewerViewController.swift**:
- `viewWillTransition()` 미구현
- 회전 감지 후 자식 VC에 알림 없음

**GridViewController.swift**:
- `viewWillTransition()` 미구현
- 스크롤 위치 보존 로직 없음
- 회전 애니메이션과 레이아웃 업데이트 동기화 안 됨
- contentInset 계산 timing issue (FloatingUI 높이 계산 전 호출될 수 있음)

**AlbumGridViewController.swift**:
- GridViewController와 동일한 문제

---

## 수정 계획

### Phase 1: PhotoPageViewController 회전 대응

**파일**: `PickPhoto/Features/Viewer/PhotoPageViewController.swift`

#### 1.1 회전 레이아웃 재계산 메서드 추가

```swift
/// 회전 후 레이아웃 재계산 (줌 스케일 유지)
private func recalculateLayoutForRotation() {
    let currentZoomScale = scrollView.zoomScale

    // 새 bounds 기준으로 aspect fit 재계산
    let containerSize = view.bounds.size
    let assetRatio = CGFloat(asset.pixelWidth) / CGFloat(asset.pixelHeight)
    let containerRatio = containerSize.width / containerSize.height

    let fitSize: CGSize
    if assetRatio > containerRatio {
        fitSize = CGSize(width: containerSize.width,
                        height: containerSize.width / assetRatio)
    } else {
        fitSize = CGSize(width: containerSize.height * assetRatio,
                        height: containerSize.height)
    }

    // 레이아웃 업데이트
    imageView.frame = CGRect(origin: .zero, size: fitSize)
    scrollView.contentSize = fitSize

    // 중앙 정렬
    let hInset = max(0, (containerSize.width - fitSize.width) / 2)
    let vInset = max(0, (containerSize.height - fitSize.height) / 2)
    scrollView.contentInset = UIEdgeInsets(top: vInset, left: hInset,
                                           bottom: vInset, right: hInset)

    // 줌 스케일 유지 또는 리셋
    if currentZoomScale > 1.0 {
        scrollView.zoomScale = 1.0  // 회전 시 줌 리셋
    }
    scrollView.contentOffset = CGPoint(x: -hInset, y: -vInset)

    updateMaxZoomScale()
}
```

#### 1.2 viewDidLayoutSubviews 수정

```swift
override func viewDidLayoutSubviews() {
    super.viewDidLayoutSubviews()

    let oldSize = scrollView.frame.size
    scrollView.frame = view.bounds

    // 초기 레이아웃 (1회)
    if !hasAppliedInitialLayout {
        applyInitialLayout()
        requestLOD0Image()
        return
    }

    // 줌 중에는 보류
    if isZoomInteractionActive {
        needsLayoutUpdateAfterZoom = true
        return
    }

    // 회전 감지: bounds 크기가 변경되었으면 레이아웃 재계산
    if oldSize != view.bounds.size && oldSize != .zero {
        recalculateLayoutForRotation()
    }
}
```

### Phase 2: ViewerViewController 회전 전파

**파일**: `PickPhoto/Features/Viewer/ViewerViewController.swift`

```swift
override func viewWillTransition(
    to size: CGSize,
    with coordinator: UIViewControllerTransitionCoordinator
) {
    super.viewWillTransition(to: size, with: coordinator)

    coordinator.animate(alongsideTransition: { [weak self] _ in
        // 회전 중: FaceButtonOverlay 숨김
        self?.faceButtonOverlay?.hideButtonsImmediately()
    }, completion: { [weak self] _ in
        // 회전 완료: FaceButtonOverlay 재표시
        self?.refreshFaceButtonsAfterRotation()
    })
}
```

### Phase 3: FaceButtonOverlay 갱신

**파일**: `PickPhoto/Features/Viewer/ViewerViewController+SimilarPhoto.swift`

```swift
/// 회전 후 +버튼 위치 갱신
private func refreshFaceButtonsAfterRotation() {
    guard shouldEnableSimilarPhoto else { return }
    faceButtonOverlay?.layoutButtons(for: view.bounds)
}
```

### Phase 4: GridViewController 회전 대응

**파일**: `PickPhoto/Features/Grid/GridViewController.swift`

```swift
override func viewWillTransition(
    to size: CGSize,
    with coordinator: UIViewControllerTransitionCoordinator
) {
    super.viewWillTransition(to: size, with: coordinator)

    // 스크롤 위치 저장 (정규화)
    let normalizedOffset = calculateNormalizedScrollPosition()

    coordinator.animate(alongsideTransition: { [weak self] _ in
        // 레이아웃 무효화
        self?.collectionView.collectionViewLayout.invalidateLayout()
    }, completion: { [weak self] _ in
        guard let self = self else { return }
        // 스크롤 위치 복원
        self.restoreNormalizedScrollPosition(normalizedOffset)
        // contentInset 재계산 (FloatingUI 높이 반영)
        self.updateContentInset()
    })
}

/// 현재 스크롤 위치를 정규화 (0~1)
private func calculateNormalizedScrollPosition() -> CGFloat {
    let contentHeight = collectionView.contentSize.height
    guard contentHeight > 0 else { return 0 }
    return collectionView.contentOffset.y / contentHeight
}

/// 정규화된 스크롤 위치 복원
private func restoreNormalizedScrollPosition(_ normalized: CGFloat) {
    let newContentHeight = collectionView.contentSize.height
    let newOffset = normalized * newContentHeight
    collectionView.contentOffset.y = max(0, newOffset)
}
```

### Phase 5: AlbumGridViewController 회전 대응

**파일**: `PickPhoto/Features/Albums/AlbumGridViewController.swift`

GridViewController와 동일한 패턴 적용.

---

## 수정 파일 목록

| 파일 | 변경 내용 |
|------|----------|
| `PhotoPageViewController.swift` | `recalculateLayoutForRotation()` 추가, `viewDidLayoutSubviews()` 수정 |
| `ViewerViewController.swift` | `viewWillTransition()` 추가 |
| `ViewerViewController+SimilarPhoto.swift` | `refreshFaceButtonsAfterRotation()` 추가 |
| `GridViewController.swift` | `viewWillTransition()` 추가, 스크롤 위치 보존 로직 |
| `AlbumGridViewController.swift` | `viewWillTransition()` 추가, 스크롤 위치 보존 로직 |

---

## 테스트 체크리스트

### 뷰어
- [ ] 일반 상태에서 세로→가로 회전
- [ ] 일반 상태에서 가로→세로 회전
- [ ] 줌 상태에서 회전 (줌 리셋 확인)
- [ ] 세로 사진 회전 시 비율 정상
- [ ] 가로 사진 회전 시 비율 정상
- [ ] +버튼 위치 회전 후 정상 (SimilarPhoto 기능)

### 그리드
- [ ] 그리드에서 세로→가로 회전
- [ ] 그리드에서 가로→세로 회전
- [ ] 스크롤 위치 보존 확인
- [ ] FloatingTitleBar/TabBar 가림 없음
- [ ] 앨범 그리드에서도 동일 테스트

---

## 구현 순서

1. **PhotoPageViewController** (뷰어 핵심)
2. **ViewerViewController** (뷰어 전파)
3. **GridViewController** (그리드)
4. **AlbumGridViewController** (앨범)
5. **FaceButtonOverlay** (선택)
