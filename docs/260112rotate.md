# 회전 시 레이아웃 깨짐 수정 계획

## 문제 요약

디바이스 회전 시 **뷰어 + 그리드** 화면에서 레이아웃이 깨지는 문제.

### 근본 원인

**PhotoPageViewController.swift** (756줄):
- `hasAppliedInitialLayout = true`로 고정되어 회전 후 `applyInitialLayout()` 재호출 안 됨
- `viewDidLayoutSubviews()`에서 `scrollView.frame`만 업데이트
- `imageView.frame`, `contentSize`, `contentInset` 회전 후 재계산 안 됨

**VideoPageViewController.swift** (동일 구조):
- PhotoPageViewController와 동일한 UIScrollView 기반 줌
- `viewDidLayoutSubviews()`에서 회전 감지 없이 `updateVideoLayout()` 호출
- 회전 시 playerLayerView 비율 재계산 필요

**ViewerViewController.swift** (1124줄):
- `viewWillTransition()` 미구현
- 회전 감지 후 자식 VC에 알림 없음
- FaceButtonOverlay 위치 갱신 로직 없음

**GridViewController.swift** (1028줄) + **GridScroll.swift**:
- `viewWillTransition()` 미구현
- 스크롤 위치 보존 로직 없음
- 회전 애니메이션과 레이아웃 업데이트 동기화 안 됨
- contentInset 계산 timing issue (FloatingUI 높이 계산 전 호출될 수 있음)

**AlbumGridViewController.swift** (460줄):
- GridViewController와 동일한 문제
- BaseGridViewController 상속 구조 활용 가능

---

## 수정 계획

### Phase 1: PhotoPageViewController 회전 대응

**파일**: `SweepPic/Features/Viewer/PhotoPageViewController.swift`

#### 1.1 회전 레이아웃 재계산 메서드 추가

기존 `applyInitialLayout()` 로직을 재활용하되, 줌 상태 처리 추가.

```swift
/// 회전 후 레이아웃 재계산 (줌 스케일 리셋)
/// - Note: applyInitialLayout()과 유사하지만 hasAppliedInitialLayout 체크 없이 실행
private func recalculateLayoutForRotation() {
    let assetWidth = CGFloat(asset.pixelWidth)
    let assetHeight = CGFloat(asset.pixelHeight)
    guard assetWidth > 0, assetHeight > 0 else { return }

    // 새 bounds 기준으로 aspect fit 재계산
    let containerSize = view.bounds.size
    let assetRatio = assetWidth / assetHeight
    let containerRatio = containerSize.width / containerSize.height

    let fitSize: CGSize
    if assetRatio > containerRatio {
        // 가로가 더 넓은 이미지 → width에 맞춤
        fitSize = CGSize(width: containerSize.width,
                        height: containerSize.width / assetRatio)
    } else {
        // 세로가 더 긴 이미지 → height에 맞춤
        fitSize = CGSize(width: containerSize.height * assetRatio,
                        height: containerSize.height)
    }

    // 레이아웃 업데이트
    imageView.frame = CGRect(origin: .zero, size: fitSize)
    scrollView.contentSize = fitSize

    // 중앙 정렬
    let hInset = max(0, (containerSize.width - fitSize.width) / 2)
    let vInset = max(0, (containerSize.height - fitSize.height) / 2)
    scrollView.contentInset = UIEdgeInsets(top: vInset, left: hInset,
                                           bottom: vInset, right: hInset)

    // 회전 시 줌 리셋 (1x로 복귀)
    scrollView.zoomScale = 1.0
    scrollView.contentOffset = CGPoint(x: -hInset, y: -vInset)

    updateMaxZoomScale()

    if debugPhoto {
        print("[Photo] 🔄 recalculateLayoutForRotation - index: \(index), fit=\(fitSize)")
    }
}
```

#### 1.2 viewDidLayoutSubviews 수정

**주의**: `oldSize`는 `scrollView.frame` 업데이트 **전에** 저장해야 함.

```swift
override func viewDidLayoutSubviews() {
    super.viewDidLayoutSubviews()

    // ⚠️ 순서 중요: 먼저 oldSize 저장 → 그 다음 frame 업데이트
    let oldSize = scrollView.frame.size
    let newSize = view.bounds.size
    scrollView.frame = view.bounds

    if debugPhoto {
        print("[Photo] 📐 viewDidLayoutSubviews - index: \(index), oldSize: \(oldSize), newSize: \(newSize)")
    }

    // 초기 레이아웃 (1회)
    if !hasAppliedInitialLayout {
        applyInitialLayout()
        requestLOD0Image()
        return
    }

    // 줌 중에는 보류
    if isZoomInteractionActive {
        needsLayoutUpdateAfterZoom = true
        return
    }

    // 회전 감지: bounds 크기가 변경되었으면 레이아웃 재계산
    // (oldSize가 .zero면 초기 상태이므로 무시)
    // Note: iPad split view 등 "회전 외 크기 변화"에서도 트리거됨 (의도된 동작)
    if oldSize != newSize && oldSize != .zero {
        recalculateLayoutForRotation()
    }
}
```

### Phase 1-B: VideoPageViewController 회전 대응

**파일**: `SweepPic/Features/Viewer/VideoPageViewController.swift`

PhotoPageViewController와 동일한 패턴 적용. UIScrollView + playerLayerView 구조.

#### 1-B.1 회전 레이아웃 재계산 메서드 추가

```swift
/// 회전 후 레이아웃 재계산 (줌 스케일 리셋)
private func recalculateLayoutForRotation() {
    guard videoDisplaySize.width > 0, videoDisplaySize.height > 0 else { return }

    let containerSize = view.bounds.size
    let videoRatio = videoDisplaySize.width / videoDisplaySize.height
    let containerRatio = containerSize.width / containerSize.height

    let fitSize: CGSize
    if videoRatio > containerRatio {
        fitSize = CGSize(width: containerSize.width,
                        height: containerSize.width / videoRatio)
    } else {
        fitSize = CGSize(width: containerSize.height * videoRatio,
                        height: containerSize.height)
    }

    // 레이아웃 업데이트
    playerLayerView.frame = CGRect(origin: .zero, size: fitSize)
    scrollView.contentSize = fitSize

    // 중앙 정렬
    let hInset = max(0, (containerSize.width - fitSize.width) / 2)
    let vInset = max(0, (containerSize.height - fitSize.height) / 2)
    scrollView.contentInset = UIEdgeInsets(top: vInset, left: hInset,
                                           bottom: vInset, right: hInset)

    // 회전 시 줌 리셋 (1x로 복귀)
    scrollView.zoomScale = 1.0
    scrollView.contentOffset = CGPoint(x: -hInset, y: -vInset)

    updateMaxZoomScale()

    if debugVideo {
        print("[Video] 🔄 recalculateLayoutForRotation - index: \(index), fit=\(fitSize)")
    }
}
```

#### 1-B.2 viewDidLayoutSubviews 수정

```swift
override func viewDidLayoutSubviews() {
    super.viewDidLayoutSubviews()

    // 줌 중에는 레이아웃 업데이트 보류
    guard !isZoomInteractionActive else { return }

    // ⚠️ 순서 중요: 먼저 oldSize 저장 → 그 다음 frame 업데이트
    let oldSize = scrollView.frame.size
    let newSize = view.bounds.size
    scrollView.frame = view.bounds

    // 회전 감지: bounds 크기가 변경되었으면 레이아웃 재계산
    if oldSize != newSize && oldSize != .zero && videoDisplaySize != .zero {
        recalculateLayoutForRotation()
    } else {
        updateVideoLayout()
    }
}
```

### Phase 2: ViewerViewController 회전 전파

**파일**: `SweepPic/Features/Viewer/ViewerViewController.swift`

`viewWillTransition` 메서드를 추가하여 회전 시 FaceButtonOverlay 위치 갱신.

```swift
// MARK: - Rotation

override func viewWillTransition(
    to size: CGSize,
    with coordinator: UIViewControllerTransitionCoordinator
) {
    super.viewWillTransition(to: size, with: coordinator)

    coordinator.animate(alongsideTransition: { [weak self] _ in
        // 회전 중: FaceButtonOverlay 즉시 숨김 (위치 오류 방지)
        self?.faceButtonOverlay?.hideButtonsImmediately()
    }, completion: { [weak self] _ in
        // 회전 완료: FaceButtonOverlay 재표시
        self?.refreshFaceButtonsAfterRotation()
    })
}

/// 회전 후 +버튼 위치 갱신
/// - Note: shouldEnableSimilarPhoto는 Extension의 private 프로퍼티이므로
///         faceButtonOverlay 존재 여부로 기능 활성화 판단
private func refreshFaceButtonsAfterRotation() {
    // faceButtonOverlay가 nil이면 SimilarPhoto 기능 비활성화 상태
    faceButtonOverlay?.layoutButtons(for: view.bounds)
}
```

### Phase 3: FaceButtonOverlay 갱신 (이미 구현됨)

**파일**: `SweepPic/Features/SimilarPhoto/UI/FaceButtonOverlay.swift`

`layoutButtons(for:)` 메서드가 이미 존재함 (359번 줄).

```swift
// FaceButtonOverlay.swift:359
func layoutButtons(for viewerFrame: CGRect) {
    // 기존 버튼들의 위치를 새 viewerFrame 기준으로 재계산
    // ...
}
```

**확인 필요**: `layoutButtons`가 현재 저장된 얼굴 정보를 기반으로 위치만 재계산하는지 확인.

### Phase 4: BaseGridViewController 회전 대응 (공통화)

**파일**: `SweepPic/Features/Grid/BaseGridViewController.swift`

GridViewController와 AlbumGridViewController가 모두 BaseGridViewController를 상속하므로,
회전 대응 코드를 Base에 추가하여 중복 제거.

#### 4.1 스크롤 앵커 프로퍼티 추가

```swift
// MARK: - Rotation Support

/// 회전 시 스크롤 위치 보존용 앵커 indexPath
/// - 화면 중앙에 있던 셀의 indexPath를 저장
private var scrollAnchorIndexPath: IndexPath?
```

#### 4.2 viewWillTransition 구현

**indexPath 기반 방식 사용** (정규화 비율보다 정확함):

```swift
override func viewWillTransition(
    to size: CGSize,
    with coordinator: UIViewControllerTransitionCoordinator
) {
    super.viewWillTransition(to: size, with: coordinator)

    // 회전 전: 화면 중앙 셀의 indexPath 저장
    saveScrollAnchorIndexPath()

    coordinator.animate(alongsideTransition: { [weak self] _ in
        // 레이아웃 무효화 (새 컬럼 수 계산됨)
        self?.collectionView.collectionViewLayout.invalidateLayout()
    }, completion: { [weak self] _ in
        guard let self = self else { return }
        // ⚠️ 순서 중요: 먼저 inset 확정 → 그 다음 스크롤
        // (inset 변경 후 스크롤하면 위치가 밀리지 않음)
        self.updateContentInset()
        self.restoreScrollAnchorIndexPath()
    })
}

/// 화면 중앙에 있는 셀의 indexPath 저장
/// - Note: 셀 간격(2pt)에 centerPoint가 걸리면 nil 반환 가능 → fallback 처리
private func saveScrollAnchorIndexPath() {
    let visibleRect = CGRect(
        origin: collectionView.contentOffset,
        size: collectionView.bounds.size
    )
    let centerPoint = CGPoint(
        x: visibleRect.midX,
        y: visibleRect.midY
    )

    // 1차 시도: 정확히 centerPoint에 있는 셀
    if let indexPath = collectionView.indexPathForItem(at: centerPoint) {
        scrollAnchorIndexPath = indexPath
        return
    }

    // 2차 시도 (fallback): visible cells 중 중앙에 가장 가까운 셀
    let visibleIndexPaths = collectionView.indexPathsForVisibleItems
    guard !visibleIndexPaths.isEmpty else { return }

    var closestIndexPath: IndexPath?
    var closestDistance: CGFloat = .greatestFiniteMagnitude

    for indexPath in visibleIndexPaths {
        guard let cell = collectionView.cellForItem(at: indexPath) else { continue }
        let cellCenter = cell.center
        let distance = hypot(cellCenter.x - centerPoint.x, cellCenter.y - centerPoint.y)
        if distance < closestDistance {
            closestDistance = distance
            closestIndexPath = indexPath
        }
    }

    scrollAnchorIndexPath = closestIndexPath
}

/// 저장된 indexPath 기준으로 스크롤 복원
private func restoreScrollAnchorIndexPath() {
    guard let indexPath = scrollAnchorIndexPath else { return }
    scrollAnchorIndexPath = nil

    // 유효성 검사
    guard indexPath.item < collectionView.numberOfItems(inSection: 0) else { return }

    // 중앙에 위치하도록 스크롤
    collectionView.scrollToItem(at: indexPath, at: .centeredVertically, animated: false)
}
```

#### 4.3 왜 indexPath 기반인가?

| 방식 | 장점 | 단점 |
|------|------|------|
| **정규화 비율** | 간단한 계산 | 회전 시 행 수 변경으로 부정확 |
| **indexPath 기반** | 정확한 위치 복원 | collectionView API 호출 필요 |

예: 세로 3열 → 가로 5열 회전 시
- 정규화 비율: 50% 위치 → 다른 사진 표시될 수 있음
- indexPath: 정확히 같은 사진이 중앙에 위치

### Phase 5: AlbumGridViewController (자동 적용)

**파일**: `SweepPic/Features/Albums/AlbumGridViewController.swift`

BaseGridViewController 상속이므로 Phase 4 구현 시 자동으로 적용됨.
추가 코드 불필요.

---

## 수정 파일 목록

| 파일 | 변경 내용 |
|------|----------|
| `PhotoPageViewController.swift` | `recalculateLayoutForRotation()` 추가, `viewDidLayoutSubviews()` 수정 |
| `VideoPageViewController.swift` | `recalculateLayoutForRotation()` 추가, `viewDidLayoutSubviews()` 수정 |
| `ViewerViewController.swift` | `viewWillTransition()` 추가, `refreshFaceButtonsAfterRotation()` 추가 |
| `BaseGridViewController.swift` | `viewWillTransition()` 추가, 스크롤 위치 보존 로직 (indexPath 기반 + fallback) |

**Note**: AlbumGridViewController, GridViewController, TrashAlbumViewController는 BaseGridViewController 상속으로 자동 적용.

---

## 테스트 체크리스트

### 뷰어 (PhotoPageViewController + ViewerViewController)
- [ ] 일반 상태에서 세로→가로 회전
- [ ] 일반 상태에서 가로→세로 회전
- [ ] 줌 상태에서 회전 (줌 1x로 리셋 확인)
- [ ] 세로 사진 회전 시 비율 정상 (레터박스 정상)
- [ ] 가로 사진 회전 시 비율 정상 (레터박스 정상)
- [ ] +버튼 위치 회전 후 정상 (SimilarPhoto 기능)
- [ ] 동영상 뷰어에서 회전 (VideoPageViewController)

### 그리드 (BaseGridViewController)
- [ ] 사진보관함에서 세로→가로 회전
- [ ] 사진보관함에서 가로→세로 회전
- [ ] 스크롤 위치 보존 확인 (중앙 사진 유지)
- [ ] FloatingTitleBar/TabBar 가림 없음
- [ ] contentInset 정상 적용

### 앨범 그리드 (AlbumGridViewController)
- [ ] 앨범에서 세로→가로 회전
- [ ] 앨범에서 가로→세로 회전
- [ ] 스크롤 위치 보존 확인

### 휴지통 (TrashAlbumViewController)
- [ ] 휴지통에서 세로→가로 회전
- [ ] 휴지통에서 가로→세로 회전

---

## 구현 순서

1. **Phase 1: PhotoPageViewController** (뷰어 이미지 레이아웃)
2. **Phase 1-B: VideoPageViewController** (뷰어 동영상 레이아웃)
3. **Phase 2: ViewerViewController** (FaceButtonOverlay 갱신)
4. **Phase 4: BaseGridViewController** (그리드 스크롤 위치 보존)

**Phase 3, 5는 별도 구현 불필요** (기존 코드 활용 또는 상속으로 자동 적용)

---

## GPT 리뷰 반영 내역

| 항목 | 심각도 | 수정 내용 |
|------|--------|----------|
| VideoPageViewController 누락 | 높음 | Phase 1-B 추가 |
| updateContentInset 순서 | 중간 | 순서 변경: `updateContentInset()` → `restoreScrollAnchorIndexPath()` |
| indexPathForItem nil 가능성 | 중간 | fallback 로직 추가: visible cells 중 가장 가까운 셀 선택 |
| 회전 외 크기 변화 | 낮음 | 의도된 동작으로 유지, Note 추가 (iPad split view 대응 가능) |

---

## 참고: 현재 코드 구조

```
BaseGridViewController (추상 베이스)
├── GridViewController (사진보관함)
├── AlbumGridViewController (앨범 상세)
└── TrashAlbumViewController (휴지통)

ViewerViewController
├── PhotoPageViewController (사진)
└── VideoPageViewController (동영상)

ViewerViewController+SimilarPhoto.swift
└── faceButtonOverlay (Associated Object)
└── shouldEnableSimilarPhoto (private)
```
