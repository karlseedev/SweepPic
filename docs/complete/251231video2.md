# VideoPageViewController AVPlayer + AVPlayerLayer 재구현 계획

## 개요

AVPlayerViewController → AVPlayer + AVPlayerLayer 전환
- 목표: 기본 사진 앱과 동일한 UX (포스터 → 동영상, 핀치줌, 더블탭)

## 현재 문제점 및 해결책

| 문제 | 해결책 |
|------|--------|
| 페이지 전환 시 검은 화면 | 포스터 먼저 표시 + isReadyForDisplay 후 전환 |
| 핀치줌 미지원 | UIScrollView + PlayerLayerView |
| 더블탭 줌 미지원 | 커스텀 제스처 구현 |

## 파일 구조

```
PickPhoto/Features/Viewer/
├── PlayerLayerView.swift             (신규)
├── VideoPageViewController.swift     (재작성)
├── VideoControlsOverlay.swift        (2차 구현)
└── ViewerViewController.swift        (수정)
```

---

## Phase 1: PlayerLayerView 생성

### 목표
`layerClass` override로 AVPlayerLayer 자동 동기화

### 파일: PlayerLayerView.swift (신규)

```swift
import UIKit
import AVFoundation

/// AVPlayerLayer를 layerClass로 사용하는 뷰
/// - 레이어 프레임이 view.bounds와 자동 동기화
/// - 포스터 이미지 오버레이 내장
final class PlayerLayerView: UIView {

    // MARK: - Layer Class

    override class var layerClass: AnyClass { AVPlayerLayer.self }

    var playerLayer: AVPlayerLayer { layer as! AVPlayerLayer }

    var player: AVPlayer? {
        get { playerLayer.player }
        set { playerLayer.player = newValue }
    }

    // MARK: - Poster Image

    private(set) lazy var posterImageView: UIImageView = {
        let iv = UIImageView()
        iv.contentMode = .scaleAspectFit
        iv.clipsToBounds = true
        return iv
    }()

    // MARK: - Loading Indicator

    private(set) lazy var loadingIndicator: UIActivityIndicatorView = {
        let indicator = UIActivityIndicatorView(style: .large)
        indicator.color = .white
        indicator.hidesWhenStopped = true
        return indicator
    }()

    // MARK: - Initialization

    override init(frame: CGRect) {
        super.init(frame: frame)
        setupUI()
    }

    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

    private func setupUI() {
        backgroundColor = .black
        playerLayer.videoGravity = .resizeAspect

        addSubview(posterImageView)
        addSubview(loadingIndicator)
    }

    override func layoutSubviews() {
        super.layoutSubviews()
        posterImageView.frame = bounds
        loadingIndicator.center = CGPoint(x: bounds.midX, y: bounds.midY)
    }

    // MARK: - Poster API

    func setPoster(_ image: UIImage?) {
        posterImageView.image = image
        posterImageView.alpha = 1
    }

    func hidePoster(animated: Bool = true) {
        let duration = animated ? 0.2 : 0
        UIView.animate(withDuration: duration) {
            self.posterImageView.alpha = 0
        }
    }

    func showPoster() {
        posterImageView.alpha = 1
    }
}
```

---

## Phase 2: VideoPageViewController 재작성

### 목표
UIScrollView + PlayerLayerView + KVO 상태 관찰

### 파일: VideoPageViewController.swift (재작성)

### 핵심 구조

```swift
final class VideoPageViewController: UIViewController {
    let asset: PHAsset
    let index: Int

    // 줌을 위한 UIScrollView
    private lazy var scrollView: UIScrollView

    // PlayerLayerView (layerClass = AVPlayerLayer)
    private lazy var playerLayerView: PlayerLayerView

    // AVPlayer
    private var player: AVPlayer?

    // KVO 관찰
    private var readyForDisplayObserver: NSKeyValueObservation?
    private var statusObserver: NSKeyValueObservation?

    // 요청 취소 토큰
    private var thumbnailRequestCancellable: Cancellable?
    private var videoRequestCancellable: Cancellable?

    // 비디오 사이즈 (줌 계산용) - preferredTransform 적용된 표시 크기
    private var videoDisplaySize: CGSize = .zero

    // 줌 상태 플래그
    private var isZoomInteractionActive = false

    // 자동 재생 플래그
    private var shouldAutoPlay = true

    // 비디오 요청 완료 여부 (중복 요청 방지)
    private var hasRequestedVideo = false
}
```

### 레이어 순서

```
view
├── scrollView
│   └── PlayerLayerView
│       ├── posterImageView (내장)
│       └── AVPlayerLayer (layerClass)
└── (loadingIndicator는 PlayerLayerView 내장)
```

### 생명주기 흐름

```
viewDidLoad
├── setupUI()
├── setupGestures()
└── loadPoster()        ← 포스터만 즉시 요청 (ImagePipeline)

viewDidLayoutSubviews
├── scrollView.frame = view.bounds
└── updateVideoLayout()     ← 회전/초기 레이아웃 대응

ViewerViewController.didFinishAnimating (외부 트리거)
└── requestVideoIfNeeded()  ← ViewerVC가 현재 페이지에만 호출

비디오 준비 완료 (completion)
├── setupPlayer(with: playerItem)
├── setupKVO()          ← isReadyForDisplay 관찰 시작 (.initial + .new)
└── fetchVideoDisplaySize()  ← preferredTransform 적용

isReadyForDisplay == true (KVO)
├── hidePoster()        ← 이때 포스터 fade-out (1회만 실행)
└── play(muted: true)   ← 자동 재생

viewWillDisappear
├── pause()
└── cancelVideoRequest()    ← 진행 중인 요청 취소 + 플래그 리셋
```

---

## Phase 3: KVO 상태 관찰

### 목표
첫 프레임 렌더링 준비 완료 후 포스터 제거 (검은 화면 방지)

### 구현

```swift
// MARK: - KVO Setup

private func setupKVO() {
    // AVPlayerLayer.isReadyForDisplay 관찰
    // .initial 옵션: 이미 true인 경우에도 콜백 수신
    readyForDisplayObserver = playerLayerView.playerLayer.observe(
        \.isReadyForDisplay,
        options: [.initial, .new]  // ⚠️ .initial 필수
    ) { [weak self] layer, change in
        guard let self = self,
              layer.isReadyForDisplay else { return }

        DispatchQueue.main.async {
            self.onReadyForDisplay()
        }
    }

    // AVPlayerItem.status 관찰 (에러 처리용)
    statusObserver = player?.currentItem?.observe(
        \.status,
        options: [.initial, .new]
    ) { [weak self] item, change in
        guard let self = self else { return }

        if item.status == .failed {
            DispatchQueue.main.async {
                self.showError(item.error?.localizedDescription ?? "재생 실패")
            }
        }
    }
}

/// 첫 프레임 표시 완료 여부 (중복 호출 방지)
private var hasShownFirstFrame = false

private func onReadyForDisplay() {
    // 중복 호출 방지
    guard !hasShownFirstFrame else { return }
    hasShownFirstFrame = true

    // 포스터 fade-out
    playerLayerView.hidePoster(animated: true)
    playerLayerView.loadingIndicator.stopAnimating()

    // 자동 재생
    if shouldAutoPlay {
        player?.play()
    }
}

private func removeKVO() {
    readyForDisplayObserver?.invalidate()
    readyForDisplayObserver = nil
    statusObserver?.invalidate()
    statusObserver = nil
}
```

---

## Phase 4: 비디오 요청 시점 최적화

### 목표
인접 페이지 불필요한 다운로드 방지

### 설계 결정
- **요청 트리거**: ViewerViewController가 `didFinishAnimating`에서 현재 페이지에만 호출
- **페이지 이탈 시**: 진행 중인 요청 취소 + 플래그 리셋 (재방문 시 재요청 허용)

### 구현

```swift
// MARK: - Video Request

/// 비디오 요청 (ViewerViewController가 호출)
/// - 인접 페이지가 아닌 현재 페이지에서만 호출됨을 보장
func requestVideoIfNeeded() {
    guard !hasRequestedVideo else { return }

    hasRequestedVideo = true
    playerLayerView.loadingIndicator.startAnimating()

    videoRequestCancellable = VideoPipeline.shared.requestPlayerItem(
        for: asset,
        progressHandler: { [weak self] progress in
            // iCloud 다운로드 진행률 (선택적 UI 표시)
        },
        completion: { [weak self] playerItem, info in
            guard let self = self else { return }

            // 취소된 경우
            if VideoPipeline.isCancelled(from: info) {
                self.hasRequestedVideo = false  // 재방문 시 재요청 허용
                return
            }

            // 에러 처리
            if let error = VideoPipeline.error(from: info) {
                self.hasRequestedVideo = false  // 재시도 허용
                self.showError(error.localizedDescription)
                return
            }

            guard let playerItem = playerItem else {
                self.hasRequestedVideo = false  // 재시도 허용
                self.showError("동영상을 로드할 수 없습니다")
                return
            }

            self.setupPlayer(with: playerItem)
        }
    )
}

/// 비디오 요청 취소 (페이지 이탈 시)
private func cancelVideoRequest() {
    videoRequestCancellable?.cancel()
    videoRequestCancellable = nil
    hasRequestedVideo = false  // 재방문 시 재요청 허용
}
```

---

## Phase 5: 핀치줌 구현

### 목표
PhotoPageViewController와 동일한 핀치줌 동작

### viewDidLayoutSubviews

```swift
override func viewDidLayoutSubviews() {
    super.viewDidLayoutSubviews()

    // 줌 중에는 레이아웃 업데이트 보류
    guard !isZoomInteractionActive else { return }

    scrollView.frame = view.bounds
    updateVideoLayout()
}

private func updateVideoLayout() {
    guard videoDisplaySize.width > 0, videoDisplaySize.height > 0 else {
        // 비디오 사이즈 없으면 전체 화면
        playerLayerView.frame = scrollView.bounds
        scrollView.contentSize = scrollView.bounds.size
        return
    }

    let scrollViewSize = scrollView.bounds.size
    guard scrollViewSize.width > 0, scrollViewSize.height > 0 else { return }

    // aspect fit 크기 계산
    let widthRatio = scrollViewSize.width / videoDisplaySize.width
    let heightRatio = scrollViewSize.height / videoDisplaySize.height
    let ratio = min(widthRatio, heightRatio)

    let fitWidth = videoDisplaySize.width * ratio
    let fitHeight = videoDisplaySize.height * ratio

    playerLayerView.frame = CGRect(x: 0, y: 0, width: fitWidth, height: fitHeight)
    scrollView.contentSize = CGSize(width: fitWidth, height: fitHeight)

    updateContentInsetForCentering()
}
```

### UIScrollViewDelegate

```swift
func viewForZooming(in scrollView: UIScrollView) -> UIView? {
    return playerLayerView
}

func scrollViewWillBeginZooming(_ scrollView: UIScrollView, with view: UIView?) {
    isZoomInteractionActive = true
}

func scrollViewDidZoom(_ scrollView: UIScrollView) {
    updateContentInsetForCentering()
}

func scrollViewDidEndZooming(_ scrollView: UIScrollView, with view: UIView?, atScale scale: CGFloat) {
    isZoomInteractionActive = false
}
```

### 최대 줌 스케일 (동적 계산 + preferredTransform)

```swift
/// 비디오 표시 크기 가져오기 (preferredTransform 적용)
/// - 세로 촬영 비디오: naturalSize는 1920x1080이지만 표시는 1080x1920
private func fetchVideoDisplaySize(from playerItem: AVPlayerItem) {
    Task {
        guard let track = try? await playerItem.asset.loadTracks(withMediaType: .video).first else {
            return
        }

        // naturalSize와 preferredTransform 동시 로드
        let naturalSize = try? await track.load(.naturalSize)
        let transform = try? await track.load(.preferredTransform)

        guard let size = naturalSize else { return }

        await MainActor.run {
            if let transform = transform {
                // preferredTransform 적용하여 실제 표시 크기 계산
                let transformedSize = size.applying(transform)
                self.videoDisplaySize = CGSize(
                    width: abs(transformedSize.width),
                    height: abs(transformedSize.height)
                )
            } else {
                self.videoDisplaySize = size
            }

            self.scrollView.maximumZoomScale = self.calculateMaxZoomScale()
            self.updateVideoLayout()
        }
    }
}

/// 비디오 해상도 기반 최대 줌 스케일 계산
private func calculateMaxZoomScale() -> CGFloat {
    let containerSize = scrollView.bounds.size
    guard videoDisplaySize.width > 0, videoDisplaySize.height > 0,
          containerSize.width > 0, containerSize.height > 0 else {
        return 4.0
    }

    let fitRatio = min(containerSize.width / videoDisplaySize.width,
                       containerSize.height / videoDisplaySize.height)
    let screenScale = UIScreen.main.scale
    let calculatedScale = screenScale / fitRatio

    // 최소 4x, 최대 50x
    return max(4.0, min(calculatedScale, 50.0))
}
```

---

## Phase 6: 더블탭 줌 구현

### 목표
더블탭으로 1x ↔ 2.5x 토글

### 구현

```swift
private static let doubleTapZoomScale: CGFloat = 2.5

private lazy var doubleTapGesture: UITapGestureRecognizer = {
    let gesture = UITapGestureRecognizer(target: self, action: #selector(handleDoubleTap(_:)))
    gesture.numberOfTapsRequired = 2
    return gesture
}()

@objc private func handleDoubleTap(_ gesture: UITapGestureRecognizer) {
    if scrollView.zoomScale > 1.0 {
        scrollView.setZoomScale(1.0, animated: true)
    } else {
        let location = gesture.location(in: playerLayerView)
        let zoomRect = CGRect(
            x: location.x - (scrollView.bounds.width / Self.doubleTapZoomScale / 2),
            y: location.y - (scrollView.bounds.height / Self.doubleTapZoomScale / 2),
            width: scrollView.bounds.width / Self.doubleTapZoomScale,
            height: scrollView.bounds.height / Self.doubleTapZoomScale
        )
        scrollView.zoom(to: zoomRect, animated: true)
    }
}
```

---

## Phase 7: 제스처 충돌 방지

### 목표
zoomScale=1일 때 페이지 스와이프 우선

### 구현

```swift
// MARK: - UIGestureRecognizerDelegate

extension VideoPageViewController: UIGestureRecognizerDelegate {

    /// 페이지 스와이프와 UIScrollView pan 충돌 방지
    /// - zoomScale=1일 때 수평 스와이프는 페이지 전환으로 전달
    func gestureRecognizerShouldBegin(_ gestureRecognizer: UIGestureRecognizer) -> Bool {
        guard gestureRecognizer == scrollView.panGestureRecognizer else {
            return true
        }

        // 줌 상태가 아니면 수평 스와이프는 페이지 전환 우선
        if scrollView.zoomScale <= 1.0 {
            let velocity = scrollView.panGestureRecognizer.velocity(in: scrollView)
            // 수평 움직임이 더 크면 페이지 스와이프로
            if abs(velocity.x) > abs(velocity.y) {
                return false
            }
        }

        return true
    }
}

// scrollView 설정 시
scrollView.panGestureRecognizer.delegate = self
```

---

## Phase 8: 메모리 관리

### 생명주기

```swift
override func viewWillDisappear(_ animated: Bool) {
    super.viewWillDisappear(animated)
    pause()
    cancelVideoRequest()  // 진행 중인 요청 취소 + 플래그 리셋
}

deinit {
    cleanupPlayer()
    thumbnailRequestCancellable?.cancel()
    videoRequestCancellable?.cancel()
}

private func cleanupPlayer() {
    removeKVO()
    player?.pause()
    player?.replaceCurrentItem(with: nil)
    playerLayerView.player = nil
    player = nil
    hasShownFirstFrame = false  // 리셋
}

// MARK: - Public API

var isZoomed: Bool {
    scrollView.zoomScale > 1.0
}

func play(muted: Bool = true) {
    player?.isMuted = muted
    player?.play()
}

func pause() {
    player?.pause()
}
```

---

## Phase 9: ViewerViewController 연동

### 수정 사항

```swift
// 페이지 전환 완료 시
func pageViewController(_ pageViewController: UIPageViewController,
                       didFinishAnimating finished: Bool,
                       previousViewControllers: [UIViewController],
                       transitionCompleted completed: Bool) {
    guard completed else { return }

    // 이전 비디오 정지
    for previousVC in previousViewControllers {
        if let videoVC = previousVC as? VideoPageViewController {
            videoVC.pause()
        }
    }

    // 현재 비디오 요청 트리거
    if let currentVC = pageViewController.viewControllers?.first as? VideoPageViewController {
        currentVC.requestVideoIfNeeded()
    }
}

// 초기 페이지 설정 시에도 호출 필요
func setupInitialPage() {
    // ... 기존 코드 ...

    // 초기 페이지가 비디오인 경우 요청 트리거
    if let videoVC = pageViewController.viewControllers?.first as? VideoPageViewController {
        videoVC.requestVideoIfNeeded()
    }
}
```

---

## Phase 10: VideoControlsOverlay (2차 구현)

### 목표
기본 사진앱 스타일의 미니멀 비디오 컨트롤 UI

### 파일: VideoControlsOverlay.swift (신규)

### 우리 앱 UI 레이아웃

```
┌─────────────────────────────────────────┐
│ <     FloatingTitleBar (기존)           │  ← 기존 상단 바
│                                         │
│              (비디오 영역)               │
│                                         │
│                                         │
│ ▶/⏸                                  🔇 │  ← 버튼 (타임라인 위)
│ ════════════════●═══════════════════    │  ← 타임라인 (비디오 영역 하단)
│                                         │
├─────────────────────────────────────────┤
│        FloatingTabBar (기존)            │  ← 기존 하단 툴바
└─────────────────────────────────────────┘
```

### UI 레이아웃 상세

| 요소 | 위치 | 크기/스타일 |
|------|------|-------------|
| **재생/일시정지** | 타임라인 위, 좌측 | SF Symbol `play.fill`/`pause.fill`, 24pt, 흰색 |
| **음소거** | 타임라인 위, 우측 | SF Symbol `speaker.fill`/`speaker.slash.fill`, 24pt, 흰색 |
| **타임라인** | 비디오 영역 하단 | 높이 4pt, 좌우 여백 16pt |

### 스타일

- **크롬 없음**: 블러 배경 제거, 플로팅 흰색 버튼
- **그림자**: 가독성을 위한 드롭 섀도우
- **자동 숨김**: 재생 중 3초 후 fade-out

### 인터랙션

| 제스처 | 동작 |
|--------|------|
| **화면 탭** | 컨트롤 표시/숨김 토글 |
| **타임라인 드래그** | 좌우 스크러빙 (수평) |
| **아래 스와이프** | 뷰어 닫기 (dismissPan) |

### 제스처 충돌 없음

| 제스처 | 방향 | 충돌 |
|--------|------|------|
| dismissPan | 수직 (아래) | ❌ 없음 |
| 타임라인 스크러빙 | 수평 (좌우) | ❌ 없음 |
| 페이지 스와이프 | 수평 (좌우) | zoomScale=1일 때 우선 |

→ **기존 제스처 코드 수정 불필요**

### 핵심 구조

```swift
final class VideoControlsOverlay: UIView {

    // MARK: - Properties

    private weak var player: AVPlayer?
    private var timeObserver: Any?
    private var hideTimer: Timer?
    private var isVisible = true

    // MARK: - UI Components

    /// 재생/일시정지 버튼 (좌측)
    private lazy var playPauseButton: UIButton

    /// 음소거 버튼 (우측)
    private lazy var muteButton: UIButton

    /// 타임라인 슬라이더 (비디오 영역 하단)
    private lazy var timelineSlider: UISlider

    /// 컨트롤 컨테이너 (버튼 + 타임라인)
    private lazy var controlsContainer: UIView
}
```

### API

```swift
// MARK: - Public API

/// 플레이어 연결
func configure(with player: AVPlayer)

/// 재생 상태 업데이트
func updatePlayPauseState(isPlaying: Bool)

/// 컨트롤 표시/숨김
func show(animated: Bool)
func hide(animated: Bool)
func toggle()

/// 타임 옵저버 정리
func cleanup()
```

### 타임 옵저버

```swift
private func setupTimeObserver() {
    let interval = CMTime(seconds: 0.5, preferredTimescale: CMTimeScale(NSEC_PER_SEC))
    timeObserver = player?.addPeriodicTimeObserver(
        forInterval: interval,
        queue: .main
    ) { [weak self] time in
        guard let self = self,
              !self.isSliderDragging else { return }

        let seconds = CMTimeGetSeconds(time)
        self.timelineSlider.value = Float(seconds)
    }
}
```

### 자동 숨김

```swift
private func startAutoHideTimer() {
    cancelAutoHideTimer()
    hideTimer = Timer.scheduledTimer(withTimeInterval: 3.0, repeats: false) { [weak self] _ in
        guard let self = self,
              self.player?.timeControlStatus == .playing else { return }
        self.hide(animated: true)
    }
}

private func cancelAutoHideTimer() {
    hideTimer?.invalidate()
    hideTimer = nil
}
```

### VideoPageViewController 통합

```swift
// VideoPageViewController에 추가

private lazy var controlsOverlay: VideoControlsOverlay = {
    let overlay = VideoControlsOverlay()
    overlay.translatesAutoresizingMaskIntoConstraints = false
    return overlay
}()

private func setupUI() {
    // ... scrollView, playerLayerView 설정 ...

    // 컨트롤 오버레이 (scrollView 위에)
    view.addSubview(controlsOverlay)
    NSLayoutConstraint.activate([
        controlsOverlay.topAnchor.constraint(equalTo: view.topAnchor),
        controlsOverlay.leadingAnchor.constraint(equalTo: view.leadingAnchor),
        controlsOverlay.trailingAnchor.constraint(equalTo: view.trailingAnchor),
        controlsOverlay.bottomAnchor.constraint(equalTo: view.bottomAnchor)
    ])

    // 싱글탭 → 컨트롤 토글
    let singleTapGesture = UITapGestureRecognizer(target: self, action: #selector(handleSingleTap))
    singleTapGesture.require(toFail: doubleTapGesture)
    controlsOverlay.addGestureRecognizer(singleTapGesture)
}

@objc private func handleSingleTap() {
    controlsOverlay.toggle()
}

private func setupPlayer(with playerItem: AVPlayerItem) {
    // ... 기존 코드 ...

    // 컨트롤 오버레이 연결
    controlsOverlay.configure(with: player!)
}
```

---

## Critical Files

| 파일 | 작업 | 단계 |
|------|------|------|
| `PlayerLayerView.swift` | 신규 생성 | 1차 |
| `VideoPageViewController.swift` | 완전 재작성 | 1차 |
| `ViewerViewController.swift` | 페이지 전환 처리 수정 | 1차 |
| `VideoControlsOverlay.swift` | 신규 생성 | 2차 |

---

## QA 체크리스트

### 1차 (포스터 + 줌)
- [ ] 좌우 스와이프 시 포스터가 먼저 밀려오는지
- [ ] 돌아왔을 때 즉시 표시되는지
- [ ] 검은 화면이 최소화되는지 (isReadyForDisplay 후 전환)
- [ ] 핀치줌이 부드럽게 동작하는지
- [ ] 더블탭 줌/줌아웃이 동작하는지
- [ ] 세로 촬영 비디오 줌 스케일이 정확한지
- [ ] zoomScale=1일 때 페이지 스와이프가 우선되는지
- [ ] 회전 시 레이아웃이 정상인지
- [ ] 인접 페이지에서 불필요한 비디오 로딩이 없는지
- [ ] 긴 동영상/iCloud 동영상에서 로딩 인디케이터 표시되는지
- [ ] 메모리 누수 없는지
- [ ] 아래 스와이프로 뷰어 닫기 동작하는지

### 2차 (컨트롤 UI)
- [x] 재생/일시정지 버튼 동작하는지
- [x] 음소거 버튼 동작하는지
- [x] 타임라인 드래그로 스크러빙 되는지
- [x] 탭으로 컨트롤 표시/숨김 토글되는지
- [x] 재생 중 3초 후 자동 숨김되는지
- [x] 타임라인 드래그와 dismissPan 충돌 없는지 (방향이 다름)
- [x] 줌 시작 시 컨트롤 숨겨지는지

---

## 주의사항

1. **ViewerViewController에서 비디오 요청 트리거** - didFinishAnimating에서 현재 페이지에만 호출
2. **페이지 이탈 시 요청 취소** - viewWillDisappear에서 취소 + 플래그 리셋 (재방문 시 재요청)
3. **isReadyForDisplay KVO 중복 방지** - hasShownFirstFrame 플래그로 1회만 실행
4. **preferredTransform 적용** - 세로 비디오 표시 크기 정확히
5. **viewDidLayoutSubviews** - 초기/회전 레이아웃 대응
6. **제스처 충돌 방지** - zoomScale=1일 때 페이지 스와이프 우선
7. **layerClass override** - CATransaction 불필요, 프레임 자동 동기화
8. **오디오 세션** - 기존 setupAudioSession() 유지

---

## 구현 완료 (2026-01-04)

### Phase 1~9: 1차 구현 완료
- PlayerLayerView.swift 생성
- VideoPageViewController.swift 재작성
- KVO 상태 관찰 (isReadyForDisplay)
- 비디오 요청 시점 최적화
- 핀치줌/더블탭 줌 구현
- 제스처 충돌 방지
- 메모리 관리
- ViewerViewController 연동

### Phase 10: 2차 구현 완료
- VideoControlsOverlay.swift 신규 생성
- 재생/일시정지 버튼, 음소거 버튼, 타임라인 슬라이더
- 싱글탭으로 컨트롤 토글
- 재생 중 3초 후 자동 숨김
- 줌 시작 시 컨트롤 숨김
- 드래그 중 실시간 스크러빙 (스크러빙 중 일시정지, 종료 후 재개)

### UI 조정
- 컨트롤 위치: bottomPadding 16 → 56 (40pt 상단으로 이동)
- 버튼 크기: 44 → 22 (50% 축소)
- 아이콘 크기: 24pt → 12pt (50% 축소)

### 커밋
- `9b12f82` feat: Phase 10 - VideoControlsOverlay 구현
