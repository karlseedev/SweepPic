# Analytics Test A: 코드 주입 전수 검증 계획

## Context

Supabase 하이브리드 Analytics 구현 완료. 수집하는 **모든 이벤트 10종 × 전체 파라미터**가 정확한 값으로 Supabase API에 도달하는지 검증한다. 각 항목에 고유한 테스트 숫자를 배정하고, 코드로 주입 → flush → API 조회로 일치 여부를 자동 확인한다.

**검증 경로**: 카운터 값 주입 → flushCounters() → sendEventBatch() → SupabaseProvider.sendBatch() → PostgREST INSERT → API SELECT → 값 일치 확인

---

## 1. 테스트 값 배정표

모든 값은 **고유하고 기억하기 쉬운 숫자**로 배정. 같은 숫자가 다른 이벤트에 있어도 이벤트명으로 구분 가능.

### 1.1 세션 카운터 (flush 경로)

| 이벤트 | 파라미터 | 테스트 값 | Supabase 전송 |
|--------|---------|:--------:|:---:|
| **session.photoViewing** | total | **17** | O |
| | fromLibrary | **10** | O |
| | fromAlbum | **5** | O |
| | fromTrash | **2** | O |
| **session.deleteRestore** | gridSwipeDelete | **9** | O |
| | gridSwipeRestore | **3** | O |
| | viewerSwipeDelete | **7** | O |
| | viewerTrashButton | **4** | O |
| | viewerRestoreButton | **2** | O |
| | fromLibrary | **14** | O |
| | fromAlbum | **11** | O |
| **session.trashViewer** | permanentDelete | **6** | O |
| | restore | **8** | O |
| **session.similarAnalysis** | completedCount | **3** | O |
| | cancelledCount | **1** | O |
| | totalGroups | **11** | O |
| | avgDurationSec | **4.7** (= 14.1 ÷ 3) | O |
| **session.errors** | photoLoad.gridThumbnail | **5** | O |
| | face.detection | **2** | O |
| | cleanup.trashMove | **1** | O |
| **session.gridPerformance** | grayShown | **42** | **X (제외)** |

### 1.2 즉시 전송 이벤트

| 이벤트 | 파라미터 | 테스트 값 | Supabase 전송 |
|--------|---------|----------|:---:|
| **app.launched** | (없음, defaultParams만) | 자동 발사 | O |
| **permission.result** | result=fullAccess, timing=settingsChange | — | **X (제외)** |
| **similar.groupClosed** | totalCount | **12** | O |
| | deletedCount | **5** | O |
| **cleanup.completed** | reachedStage | **cleanupDone** | O |
| | trashWarningShown | **true** | O |
| | foundCount | **23** | O |
| | durationSec | **45.3** | O |
| | method | **fromLatest** | O |
| | result | **completed** | O |
| | cancelProgress | (nil → 미전송) | — |
| | resultAction | (nil → 미전송) | — |
| **cleanup.previewCompleted** | reachedStage | **finalAction** | O |
| | foundCount | **15** | O |
| | durationSec | **28.7** | O |
| | maxStageReached | **standard** | O |
| | expandCount | **4** | O |
| | excludeCount | **2** | O |
| | viewerOpenCount | **3** | O |
| | finalAction | **moveToTrash** | O |
| | movedCount | **11** | O |

### 1.3 Supabase 제외 검증 (음성 테스트)

| 이벤트 | 기대 결과 | 제외 이유 |
|--------|----------|----------|
| `permission.result` | Supabase에 **없어야** 함 | supabaseExcluded 목록 |
| `session.gridPerformance` | Supabase에 **없어야** 함 | supabaseExcluded 목록 |

---

## 2. 구현: AnalyticsTestInjector

**신규 파일**: `PickPhoto/PickPhoto/Debug/AnalyticsTestInjector.swift` (~130줄, `#if DEBUG` 전체)

### 2.1 핵심 로직

```swift
#if DEBUG
final class AnalyticsTestInjector {

    static let launchArgument = "--analytics-test-inject"

    /// Launch argument 확인
    static var shouldRun: Bool {
        ProcessInfo.processInfo.arguments.contains(launchArgument)
    }

    /// SceneDelegate.showMainInterface()에서 호출
    static func runIfNeeded() {
        guard shouldRun else { return }
        Log.print("[AnalyticsTest] 테스트 주입 모드 감지")
        // SDK 초기화 + UI 안정화 대기 후 실행
        DispatchQueue.main.asyncAfter(deadline: .now() + 2.0) {
            injectAndFlush()
        }
    }

    private static func injectAndFlush() {
        let service = AnalyticsService.shared

        // ── 1) 즉시 전송 이벤트 (4건 직접 발사 + app.launched 1건 자동) ──
        triggerImmediateEvents(service)

        // ── 2) 세션 카운터 주입 (barrier write) ──
        service.queue.async(flags: .barrier) {
            injectSessionCounters(&service.counters)
            Log.print("[AnalyticsTest] 세션 카운터 주입 완료")

            // ── 3) 메인 스레드에서 flush (barrier 외부) ──
            DispatchQueue.main.asyncAfter(deadline: .now() + 1.0) {
                service.handleSessionEnd()
                Log.print("[AnalyticsTest] flush 실행 완료")
            }
        }
    }
}
#endif
```

### 2.2 즉시 전송 이벤트 발사

```swift
private static func triggerImmediateEvents(_ service: AnalyticsService) {
    // app.launched → sceneWillEnterForeground에서 자동 발사 (별도 호출 불필요)

    // permission.result (Supabase 제외 대상 — 음성 테스트용)
    service.trackPermissionResult(result: .fullAccess, timing: .settingsChange)

    // similar.groupClosed
    service.trackSimilarGroupClosed(totalCount: 12, deletedCount: 5)

    // cleanup.completed
    service.trackCleanupCompleted(data: CleanupEventData(
        reachedStage: .cleanupDone,
        trashWarningShown: true,
        method: .fromLatest,
        result: .completed,
        foundCount: 23,
        durationSec: 45.3,
        cancelProgress: nil,
        resultAction: nil
    ))

    // cleanup.previewCompleted
    service.trackPreviewCleanupCompleted(data: PreviewCleanupEventData(
        reachedStage: .finalAction,
        foundCount: 15,
        durationSec: 28.7,
        maxStageReached: .standard,
        expandCount: 4,
        excludeCount: 2,
        viewerOpenCount: 3,
        finalAction: .moveToTrash,
        movedCount: 11
    ))
    Log.print("[AnalyticsTest] 즉시 전송 이벤트 4건 발사")
}
```

### 2.3 세션 카운터 주입

```swift
private static func injectSessionCounters(_ counters: inout SessionCounters) {
    counters.photoViewing = .init(total: 17, fromLibrary: 10, fromAlbum: 5, fromTrash: 2)
    counters.deleteRestore = .init(
        gridSwipeDelete: 9, gridSwipeRestore: 3,
        viewerSwipeDelete: 7, viewerTrashButton: 4, viewerRestoreButton: 2,
        fromLibrary: 14, fromAlbum: 11
    )
    counters.trashViewer = .init(permanentDelete: 6, restore: 8)
    counters.similarAnalysis = .init(
        completedCount: 3, cancelledCount: 1,
        totalGroups: 11, totalDuration: 14.1  // avg = 14.1 / 3 = 4.7
    )
    counters.errors = [
        "photoLoad.gridThumbnail": 5,
        "face.detection": 2,
        "cleanup.trashMove": 1,
    ]
    counters.gridPerformance = .init(grayShown: 42)
}
```

### 2.4 호출 지점

**SceneDelegate.swift** `showMainInterface()` 내부 기존 `#if DEBUG` 블록에 추가:

```swift
#if DEBUG
AnalyticsTestInjector.runIfNeeded()
#endif
```

---

## 3. 실행 방법 (CLI 자동화)

### 3.1 전제 조건

- Supabase 프로젝트 active 상태 (7일 미사용 시 pause → Dashboard에서 Resume 필요)
- `scripts/analytics/.env`에 SUPABASE_URL, SUPABASE_SERVICE_KEY 설정 완료
- `PickPhoto/PickPhoto/Config/Supabase.xcconfig`에 URL, ANON_KEY 설정 완료

### 3.2 실행 스크립트

```bash
#!/bin/bash
# scripts/analytics/run-test-inject.sh
# Analytics Test A: 코드 주입 → flush → 검증

set -euo pipefail
cd "$(dirname "$0")/../.."  # 프로젝트 루트

BUNDLE_ID="com.karl.PickPhoto"
SIMULATOR="iPhone 16"

echo "=== Step 1: 빌드 + 설치 ==="
# -destination 지정 빌드 후, 빌드 산출물을 시뮬레이터에 설치
xcodebuild build \
  -project PickPhoto/PickPhoto.xcodeproj \
  -scheme PickPhoto \
  -destination "platform=iOS Simulator,name=$SIMULATOR" \
  -configuration Debug

# 빌드 산출물 경로 획득 → 시뮬레이터 설치
APP_PATH=$(xcodebuild -showBuildSettings \
  -project PickPhoto/PickPhoto.xcodeproj \
  -scheme PickPhoto \
  -destination "platform=iOS Simulator,name=$SIMULATOR" \
  -configuration Debug 2>/dev/null \
  | grep -m1 "BUILT_PRODUCTS_DIR" | awk '{print $3}')
xcrun simctl install booted "$APP_PATH/PickPhoto.app"

echo ""
echo "=== Step 2: 시뮬레이터 준비 ==="
xcrun simctl boot "$SIMULATOR" 2>/dev/null || true
# 부팅 완료 대기 (privacy grant/launch 간헐 실패 방지)
xcrun simctl bootstatus booted -b
# 사진 권한 사전 부여 (PermissionViewController 우회)
xcrun simctl privacy booted grant photos "$BUNDLE_ID"

echo ""
echo "=== Step 3: 앱 실행 (테스트 주입 모드) ==="
# 기존 인스턴스 종료 후 재실행
xcrun simctl terminate booted "$BUNDLE_ID" 2>/dev/null || true
xcrun simctl launch booted "$BUNDLE_ID" --analytics-test-inject

echo ""
echo "=== Step 4: 전송 완료 대기 (12초) ==="
# SDK 초기화 2초 + 주입 + flush 1초 + HTTP 전송 완료 여유 + 네트워크 지연 버퍼
sleep 12

echo ""
echo "=== Step 5: 검증 ==="
scripts/analytics/verify-test-inject.sh
```

---

## 4. 검증 스크립트

**신규 파일**: `scripts/analytics/verify-test-inject.sh` (~100줄)

### 4.1 핵심 설계

- **시간 필터**: 최근 2분 내 이벤트만 조회 (이전 테스트 잔여 데이터 배제)
- **양성 테스트**: 8개 이벤트 × 전체 파라미터 값 일치 확인
- **음성 테스트**: 제외 이벤트 2종이 Supabase에 없는지 확인
- **결과 요약**: PASS/FAIL 카운트 + 실패 항목 상세

### 4.2 스크립트

```bash
#!/bin/bash
# scripts/analytics/verify-test-inject.sh
# Analytics Test A 검증: Supabase에 도착한 값 vs 배정표 비교

set -euo pipefail
source "$(dirname "$0")/.env"

# 최근 2분 이내 이벤트만 조회 (이전 테스트 잔여 데이터 배제)
SINCE=$(date -u -v-2M '+%Y-%m-%dT%H:%M:%SZ' 2>/dev/null \
     || date -u -d '2 minutes ago' '+%Y-%m-%dT%H:%M:%SZ')

BASE="$SUPABASE_URL/rest/v1/events"
AUTH=(-H "apikey: $SUPABASE_SERVICE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_KEY")

PASS=0
FAIL=0

# Supabase API 호출 헬퍼 (HTTP 에러 검출)
sb_fetch() {
    local response
    response=$(curl -fsS "$1" "${AUTH[@]}" 2>&1) || {
        echo "  ⚠️  API 호출 실패: $1"
        echo "     $response"
        return 1
    }
    echo "$response"
}

# 특정 이벤트의 params에서 값 추출 → 기대값 비교
check() {
    local event="$1" key="$2" expected="$3"
    local actual response
    response=$(sb_fetch "$BASE?event_name=eq.$event&created_at=gte.$SINCE&order=created_at.desc&limit=1") || {
        FAIL=$((FAIL + 1)); return
    }
    actual=$(echo "$response" | jq -r ".[0].params.\"$key\" // empty")

    if [ "$actual" = "$expected" ]; then
        printf "  ✅ %-35s = %s\n" "$event.$key" "$actual"
        PASS=$((PASS + 1))
    else
        printf "  ❌ %-35s = %s (기대: %s)\n" "$event.$key" "${actual:-NULL}" "$expected"
        FAIL=$((FAIL + 1))
    fi
}

# 이벤트 존재 여부 확인
check_exists() {
    local event="$1"
    local count response
    response=$(sb_fetch "$BASE?event_name=eq.$event&created_at=gte.$SINCE&limit=1") || {
        FAIL=$((FAIL + 1)); return
    }
    count=$(echo "$response" | jq 'if type == "array" then length else 0 end')

    if [ "$count" -gt 0 ]; then
        printf "  ✅ %-35s 존재\n" "$event"
        PASS=$((PASS + 1))
    else
        printf "  ❌ %-35s 미존재\n" "$event"
        FAIL=$((FAIL + 1))
    fi
}

# 이벤트 부재 확인 (음성 테스트)
check_absent() {
    local event="$1"
    local count response
    response=$(sb_fetch "$BASE?event_name=eq.$event&created_at=gte.$SINCE&limit=1") || {
        FAIL=$((FAIL + 1)); return
    }
    count=$(echo "$response" | jq 'if type == "array" then length else 0 end')

    if [ "$count" -eq 0 ]; then
        printf "  ✅ %-35s Supabase 미전송 확인\n" "$event"
        PASS=$((PASS + 1))
    else
        printf "  ❌ %-35s Supabase에 존재 (제외 대상인데 전송됨!)\n" "$event"
        FAIL=$((FAIL + 1))
    fi
}

echo "=== Analytics Test A: 코드 주입 검증 ==="
echo "    조회 범위: $SINCE 이후"
echo ""

echo "[즉시 전송 이벤트]"
check_exists "app.launched"
check "similar.groupClosed"         "totalCount"       "12"
check "similar.groupClosed"         "deletedCount"     "5"
check "cleanup.completed"           "reachedStage"     "cleanupDone"
check "cleanup.completed"           "trashWarningShown" "true"
check "cleanup.completed"           "foundCount"       "23"
check "cleanup.completed"           "durationSec"      "45.3"
check "cleanup.completed"           "method"           "fromLatest"
check "cleanup.completed"           "result"           "completed"
check "cleanup.previewCompleted"    "reachedStage"     "finalAction"
check "cleanup.previewCompleted"    "foundCount"       "15"
check "cleanup.previewCompleted"    "durationSec"      "28.7"
check "cleanup.previewCompleted"    "maxStageReached"  "standard"
check "cleanup.previewCompleted"    "expandCount"      "4"
check "cleanup.previewCompleted"    "excludeCount"     "2"
check "cleanup.previewCompleted"    "viewerOpenCount"  "3"
check "cleanup.previewCompleted"    "finalAction"      "moveToTrash"
check "cleanup.previewCompleted"    "movedCount"       "11"

echo ""
echo "[세션 카운터]"
check "session.photoViewing"        "total"            "17"
check "session.photoViewing"        "fromLibrary"      "10"
check "session.photoViewing"        "fromAlbum"        "5"
check "session.photoViewing"        "fromTrash"        "2"
check "session.deleteRestore"       "gridSwipeDelete"  "9"
check "session.deleteRestore"       "gridSwipeRestore" "3"
check "session.deleteRestore"       "viewerSwipeDelete" "7"
check "session.deleteRestore"       "viewerTrashButton" "4"
check "session.deleteRestore"       "viewerRestoreButton" "2"
check "session.deleteRestore"       "fromLibrary"      "14"
check "session.deleteRestore"       "fromAlbum"        "11"
check "session.trashViewer"         "permanentDelete"  "6"
check "session.trashViewer"         "restore"          "8"
check "session.similarAnalysis"     "completedCount"   "3"
check "session.similarAnalysis"     "cancelledCount"   "1"
check "session.similarAnalysis"     "totalGroups"      "11"
check "session.similarAnalysis"     "avgDurationSec"   "4.7"
check "session.errors"              "photoLoad.gridThumbnail" "5"
check "session.errors"              "face.detection"   "2"
check "session.errors"              "cleanup.trashMove" "1"

echo ""
echo "[Supabase 제외 확인 (음성 테스트)]"
check_absent "permission.result"
check_absent "session.gridPerformance"

echo ""
echo "======================================="
echo "  결과: $PASS 통과 / $FAIL 실패 (총 $((PASS + FAIL)) 항목)"
echo "======================================="

[ "$FAIL" -eq 0 ] && exit 0 || exit 1
```

---

## 5. 파일 변경 요약

| 구분 | 파일 | 예상 줄 |
|------|------|:------:|
| **신규** | `PickPhoto/PickPhoto/Debug/AnalyticsTestInjector.swift` | ~130줄 |
| **신규** | `scripts/analytics/run-test-inject.sh` | ~35줄 |
| **신규** | `scripts/analytics/verify-test-inject.sh` | ~100줄 |
| **수정** | `PickPhoto/App/SceneDelegate.swift` | +3줄 |

**총 신규 3개 / 수정 1개**

---

## 6. 검증 항목 총괄 (40개)

| 구분 | 항목 수 | 내용 |
|------|:------:|------|
| 즉시 전송 이벤트 존재 | 1 | app.launched |
| 즉시 전송 파라미터 | 17 | similar.groupClosed(2) + cleanup.completed(6) + cleanup.previewCompleted(9) |
| 세션 카운터 파라미터 | 20 | photoViewing(4) + deleteRestore(7) + trashViewer(2) + similarAnalysis(4) + errors(3) |
| 음성 테스트 (제외 확인) | 2 | permission.result + session.gridPerformance |
| **합계** | **40** | |

---

## 7. 주의사항

1. **Supabase 프로젝트 활성 확인**: 7일 미사용 시 자동 pause → Dashboard에서 Resume 후 테스트
2. **시뮬레이터 사진 권한**: `xcrun simctl privacy booted grant photos` 필수 (미부여 시 showMainInterface 미호출 → 테스트 실행 안 됨)
3. **반복 실행**: 시간 필터(최근 2분)로 이전 잔여 데이터 배제. 단, 2분 이내 재실행 시 중복 가능 → 재실행 전 간격 두기
4. **onFlushComplete nil**: 테스트에서 직접 handleSessionEnd() 호출 시 onFlushComplete 미설정 → nil이므로 무해 (Supabase 배치 전송은 정상 수행, completion만 no-op)
5. **테스트 후 정리**: SceneDelegate의 `AnalyticsTestInjector.runIfNeeded()` 호출은 launch argument 조건부이므로 일반 실행에 영향 없음. 별도 정리 불필요.
