# Analytics Test B: XCUITest E2E 검증 계획 (실기기)

## 테스트 결과 (2026-03-02)

### 자동 테스트: XCUITest (실기기)

**Phase 1: 유사 사진 (testPhase1_similarPhoto)** ✅

| 이벤트 | 파라미터 | 기대값 | 실제값 | 결과 |
|--------|---------|:------:|:------:|:----:|
| similar.groupClosed | totalCount | ≥1 | 검증 완료 | ✅ |
| | deletedCount | ≥1 | 검증 완료 | ✅ |
| session.similarAnalysis | completedCount | ≥1 | 검증 완료 | ✅ |

**Phase 2: 사진 열람 (testPhase2_photoViewing)** ✅

| 이벤트 | 파라미터 | 기대값 | 실제값 | 결과 |
|--------|---------|:------:|:------:|:----:|
| session.photoViewing | total | 8 | 8 | ✅ |
| | fromLibrary | 7 | 7 | ✅ |
| | fromAlbum | 0 | 0 | ✅ |
| | fromTrash | 1 | 1 | ✅ |

**Phase 3: 뷰어 삭제 (testPhase3_viewerDelete)** ✅

| 이벤트 | 파라미터 | 기대값 | 실제값 | 결과 |
|--------|---------|:------:|:------:|:----:|
| session.deleteRestore | viewerTrashButton | 3 | 3 | ✅ |
| | viewerSwipeDelete | 2 | 2 | ✅ |
| | fromLibrary | 5 | 5 | ✅ |

**Phase 4: 목록에서 밀어서 삭제 (수동)** ✅

> XCUITest의 `press(thenDragTo:withVelocity:)` 제스처가 앱의 UIPanGestureRecognizer에 인식되지 않아 수동 테스트로 대체

| 이벤트 | 파라미터 | 기대값 | 실제값 | 결과 |
|--------|---------|:------:|:------:|:----:|
| session.deleteRestore | gridSwipeDelete | 7 | 7 | ✅ |
| | fromLibrary | 7 | 7 | ✅ |

**Phase 5: 삭제대기함 (testPhase5_trashViewer)** ✅

| 이벤트 | 파라미터 | 기대값 | 실제값 | 결과 |
|--------|---------|:------:|:------:|:----:|
| session.trashViewer | permanentDelete | 2 | 2 | ✅ |
| | restore | 3 | 3 | ✅ |
| session.deleteRestore | viewerRestoreButton | 3 | 3 | ✅ |

**Phase 6: 앨범 열람 (testPhase6_albumViewing)** ✅

> idle timeout(60s)으로 스와이프 애니메이션 일부 누락, fromAlbum 카운터 정상 동작 확인

| 이벤트 | 파라미터 | 기대값 | 실제값 | 결과 |
|--------|---------|:------:|:------:|:----:|
| session.photoViewing | total | 3 | 2 | ⚠️ |
| | fromAlbum | 3 | 2 | ⚠️ |
| session.deleteRestore | gridSwipeDelete | 1 | 1 | ✅ |
| | fromAlbum | 1 | 1 | ✅ |

> ⚠️ Phase 6의 photoViewing 차이는 MTKView(LiquidGlassKit) 연속 렌더링으로 인한 XCUITest idle timeout(60s)이 원인. 카운팅 로직 자체는 정상.

---

### 수동 테스트: 정리 플로우 (cleanup)

버그 5건 수정 후 재검증 완료.

**라운드 1: 경고 알럿 (삭제대기함 비어있지 않은 상태)**

| 이벤트 | 파라미터 | 기대값 | 실제값 | 결과 |
|--------|---------|:------:|:------:|:----:|
| cleanup.completed | reachedStage | trashWarningExit | trashWarningExit | ✅ |
| | trashWarningShown | true | true | ✅ |
| | method | null | (없음) | ✅ |
| | result | null | (없음) | ✅ |
| | resultAction | null | (없음) | ✅ |

**라운드 2: 전체 흐름 (삭제대기함 비운 후, 이어하기)**

| 이벤트 | 파라미터 | 기대값 | 실제값 | 결과 |
|--------|---------|:------:|:------:|:----:|
| cleanup.previewCompleted | maxStageReached | deep | deep | ✅ |
| | expandCount | 2 | 2 | ✅ |
| | collapseCount | 1 | 1 | ✅ |
| | excludeCount | 3 | 3 | ✅ |
| | viewerOpenCount | 1 | 1 | ✅ |
| | finalAction | moveToTrash | moveToTrash | ✅ |
| cleanup.completed | reachedStage | resultAction | resultAction | ✅ |
| | trashWarningShown | false | false | ✅ |
| | result | completed | completed | ✅ |
| | resultAction | confirm | confirm | ✅ |

**수정된 버그 (커밋 3ae6a36)**:
1. 7-1: `reachedStage` `.cleanupDone` → `.resultAction` (사용자 확인 완료 시)
2. 7-1: `resultAction` 미설정 → `.confirm` 설정 추가
3. 7-2: `excludeCount` 항상 0 → `analyticsExcludeCount` 별도 카운터
4. 7-2: `collapseCount` 누락 → 신규 필드 추가
5. 7-2: `maxStageReached` currentStage 사용 → `analyticsMaxStage` (최대값만 추적)

---

## Context

테스트 A(코드 주입)는 "카운터 값 → flush → Supabase 전송" 경로를 검증한다. 테스트 B는 **실제 UI 동작 → Analytics 카운터 증가 → 세션 종료(flush) → Supabase 전송**의 전체 E2E 경로를 실기기(iPhone 13 Pro)에서 검증한다.

**자동 테스트 (XCUITest)**: 사진 열람, 삭제/복구, 삭제대기함, 유사 사진
**수동 테스트**: 정리(cleanup) — 수동 수행 후 Supabase 검증 스크립트로 확인

**검증 경로**: UI 제스처 → Analytics 카운팅 함수 호출 → SessionCounters 증가 → sceneDidEnterBackground → handleSessionEnd() → flushCounters() → sendEventBatch() → SupabaseProvider → PostgREST INSERT → API SELECT → 값 일치 확인

**타겟 기기**: iPhone 13 Pro (UDID: `00008110-00041DDC212A801E`, iOS 18.6.2)

---

## 1. 검증 범위

### 1.1 자동 테스트 (XCUITest)

| 이벤트 | 검증 항목 | 자동화 방법 |
|--------|----------|------------|
| **session.photoViewing** | total, fromLibrary, fromTrash | 셀 탭(뷰어 열기) + 좌우 스와이프(페이지 전환) |
| **session.deleteRestore** | gridSwipeDelete, viewerSwipeDelete, viewerTrashButton, viewerRestoreButton, fromLibrary | 그리드 수평 스와이프 + 뷰어 위로 스와이프 + 뷰어 버튼 탭 |
| **session.trashViewer** | permanentDelete, restore | 삭제대기함 뷰어에서 버튼 탭 |
| **similar.groupClosed** | totalCount, deletedCount | 뷰어 +버튼 → 비교 화면 → 사진 선택 → 삭제 |
| **session.similarAnalysis** | 존재 확인 (completedCount ≥ 1) | 앱 시작 시 자동 분석 → 세션 종료 시 flush |

### 1.2 수동 테스트 (정리 플로우)

| 이벤트 | 검증 방법 |
|--------|----------|
| **cleanup.completed** | 수동으로 정리 플로우 수행 → 앱 background → verify 스크립트로 확인 |
| **cleanup.previewCompleted** | 수동으로 미리보기 정리 수행 → 앱 background → verify 스크립트로 확인 |

### 1.3 제외 (테스트 A에서 검증 완료)

| 이벤트 | 이유 |
|--------|------|
| session.errors | 의도적 오류 발생 불가 |
| app.launched | 앱 실행 시 자동 (테스트 A 검증 완료) |
| permission.result | Supabase 제외 대상 (테스트 A 검증 완료) |
| session.gridPerformance | Supabase 제외 대상 (테스트 A 검증 완료) |

---

## 2. 실기기 사전 준비

### 2.1 사진 세팅 가이드

기대값에 맞는 사진을 기기에 준비한다. **순서가 중요하다.**

| 순서 | 사진 종류 | 수량 | 설명 |
|:----:|----------|:----:|------|
| **먼저** | 일반 사진 | **15장** | 풍경, 음식 등 **인물 없는** 사진 |
| **나중에** | 같은 인물 셀카 | **5장** | 동일 인물, 연속 촬영 (비슷한 배경/조명) |

> **중요**: 인물 셀카 5장을 **마지막에** 저장해야 한다. 가장 최근 사진이 그리드 맨 앞(셀 0~4)에 표시되므로, Phase 1(유사 사진)에서 바로 접근할 수 있다.

**사진 추가 방법**: AirDrop, iCloud 동기화, 또는 직접 촬영

### 2.2 기타 사전 조건

| # | 작업 | 방법 |
|---|------|------|
| 1 | **앱 사진 권한 허용** | 설정 → PickPhoto → 사진 → "모든 사진" |
| 2 | **코치마크 완료** | 앱을 한 번 실행하여 모든 코치마크 확인 완료 (이미 완료 상태) |
| 3 | **USB 연결** | Mac ↔ iPhone USB 연결 + Xcode 신뢰 설정 |
| 4 | **개발자 모드** | 설정 → 개인정보 보호 및 보안 → 개발자 모드 활성화 |

> ⚠️ **완전삭제 주의**: Phase 5에서 permanentDelete 2회 실행 시 **실제 iOS Photos에서 영구 삭제**. 중요 사진이 아닌 테스트용 사진만 사용할 것.

---

## 3. 테스트 시나리오 & 기대값 배정표

### 3.1 Phase별 시나리오

> **Phase 순서 설계 근거**: 유사 사진 테스트를 **먼저** 수행하여 이후 삭제 Phase에서 유사 그룹이 깨지는 것을 방지.

#### Phase 1: 유사 사진 (similar.groupClosed)

| Step | UI 동작 | 카운터 변화 |
|:----:|---------|-----------|
| 1 | 보관함 그리드에서 셀 0 탭 → 뷰어 열림 | photoViewing: total +1, fromLibrary +1 |
| 2 | +버튼 대기 (최대 5초) → 탭 | (변화 없음) |
| 3 | 비교 화면에서 셀 0, 1 탭 (2장 선택) | (변화 없음) |
| 4 | 삭제 버튼 탭 | similar.groupClosed(totalCount=5, deletedCount=2) 즉시 전송 |
| 5 | 비교 화면 + 뷰어 자동 dismiss → 그리드 복귀 | (변화 없음) |

> **카운터 호출 경로**: +버튼 탭 → `FaceComparisonViewController` modal → Delete → `faceComparisonViewController(_:didDeletePhotos:)` → `trackSimilarGroupClosed(totalCount:5, deletedCount:2)` 즉시 전송 + TrashStore.moveToTrash 2장 → 뷰어+비교화면 dismiss
>
> **+버튼 접근**: `accessibilityLabel = "인물 N 비교"` → `app.buttons.matching(NSPredicate(format: "label CONTAINS '비교'")).firstMatch`

→ **누적**: photoViewing total=1, fromLibrary=1 / similar.groupClosed 즉시 전송 완료

#### Phase 2: 사진 열람 (photoViewing)

| Step | UI 동작 | 카운터 변화 |
|:----:|---------|-----------|
| 6 | 보관함 그리드에서 셀 0 탭 → 뷰어 열림 | total +1, fromLibrary +1 |
| 7-11 | 뷰어에서 왼쪽 스와이프 5회 (페이지 전환) | total +5, fromLibrary +5 |
| 12 | 뒤로 버튼 탭 → 뷰어 닫기 | (변화 없음) |

→ **누적**: total=7, fromLibrary=7

#### Phase 3: 뷰어 삭제 (viewerTrashButton, viewerSwipeDelete)

| Step | UI 동작 | 카운터 변화 |
|:----:|---------|-----------|
| 13 | 보관함 그리드에서 셀 탭 → 뷰어 열림 | total +1, fromLibrary +1 |
| 14-16 | 뷰어 삭제 버튼(🗑) 탭 3회 | viewerTrashButton +3, fromLibrary +3 |
| 17-18 | 뷰어 위로 스와이프 2회 | viewerSwipeDelete +2, fromLibrary +2 |
| 19 | 뒤로 버튼 탭 → 뷰어 닫기 | (변화 없음) |

> **카운터 동작 근거**: 삭제 후 자동 전환은 `moveToNextAfterDelete()` → `setViewControllers(animated: true)` → Apple 공식 문서상 프로그래밍적 전환에서는 `didFinishAnimating` 미호출 → **countPhotoViewed 미호출** (뷰어 열기 1회만 카운트)

→ **누적**: photoViewing total=8, fromLibrary=8 / deleteRestore: viewerTrashButton=3, viewerSwipeDelete=2, fromLibrary=5

#### Phase 4: 목록에서 밀어서 삭제 (gridSwipeDelete)

| Step | UI 동작 | 카운터 변화 |
|:----:|---------|-----------|
| 20-23 | 보관함 그리드에서 셀 수평 스와이프 4회 | gridSwipeDelete +4, fromLibrary +4 |

→ **누적**: deleteRestore: gridSwipeDelete=4, fromLibrary=9

#### Phase 5: 삭제대기함 (trashViewer)

총 삭제 수: P1에서 2장 + P3에서 5장 + P4에서 4장 = **11장** → 삭제대기함에 11장

| Step | UI 동작 | 카운터 변화 |
|:----:|---------|-----------|
| 24 | 탭바 → 삭제대기함 탭 (`app.buttons["삭제대기함"]`) | (변화 없음) |
| 25 | 삭제대기함 그리드에서 셀 탭 → 뷰어 열림 | total +1, fromTrash +1 |
| 26-27 | 완전삭제 버튼 탭 2회 | trashViewer.permanentDelete +2 |
| 28-30 | 복구 버튼 탭 3회 | trashViewer.restore +3, deleteRestore.viewerRestoreButton +3 |
| 31 | 뒤로 버튼 탭 → 뷰어 닫기 | (변화 없음) |

> **카운터 호출 경로 검증 완료**:
> - permanentDeleteButton → delegate → `TrashAlbumViewController:676` → `countTrashPermanentDelete()`
> - restoreButton → `ViewerViewController:767` → `countViewerRestoreButton(source: nil)` + delegate → `TrashAlbumViewController:666` → `countTrashRestore()`
> - `coordinator.deleteSource = nil` → fromLibrary/fromAlbum **미증가**

→ **누적**: photoViewing total=9, fromTrash=1 / trashViewer: permanentDelete=2, restore=3 / deleteRestore: viewerRestoreButton=3

#### Phase 6: 세션 종료

| Step | UI 동작 | 효과 |
|:----:|---------|------|
| 32 | `XCUIDevice.shared.press(.home)` | sceneDidEnterBackground → handleSessionEnd() → flushCounters() → Supabase 배치 전송 |

### 3.2 최종 기대값 배정표 — 자동 테스트 (XCUITest)

| 이벤트 | 파라미터 | 기대값 | 산출 근거 |
|--------|---------|:------:|----------|
| **session.photoViewing** | total | **9** | P1:1 + P2:6 + P3:1 + P5:1 |
| | fromLibrary | **8** | P1:1 + P2:6 + P3:1 |
| | fromAlbum | **0** | 앨범 테스트 미실행 |
| | fromTrash | **1** | P5:1 |
| **session.deleteRestore** | gridSwipeDelete | **4** | P4:4 |
| | gridSwipeRestore | **0** | 복구 제스처 미실행 |
| | viewerSwipeDelete | **2** | P3:2 |
| | viewerTrashButton | **3** | P3:3 |
| | viewerRestoreButton | **3** | P5:3 |
| | fromLibrary | **9** | P3:5 + P4:4 |
| | fromAlbum | **0** | 앨범 테스트 미실행 |
| **session.trashViewer** | permanentDelete | **2** | P5:2 |
| | restore | **3** | P5:3 |
| **similar.groupClosed** | totalCount | **5** | P1: 비교 화면 총 사진 수 |
| | deletedCount | **2** | P1: 선택 후 삭제한 수 |
| **session.similarAnalysis** | completedCount | **≥ 1** | 앱 자동 분석 (정확한 값 불확정) |
| | totalGroups | **≥ 1** | 유사 그룹 1개 이상 |

**자동 테스트 검증 항목: 18개** (정확한 값 14개 + similar.groupClosed 2개 + similarAnalysis 존재 확인 2개)

### 3.3 수동 테스트 — 정리 (cleanup)

수동 테스트는 정확한 기대값 배정 대신 **이벤트 도착 확인 + 파라미터 값 출력**으로 검증한다.

**수동 테스트 가이드:**

| 테스트 | 수행 동작 | 검증 방법 |
|--------|----------|----------|
| **cleanup.completed** | 보관함 → 정리 시작 → "최신부터" → 정리 완료 | `verify-test-b.sh --manual` 로 이벤트 존재 + 파라미터 출력 |
| **cleanup.previewCompleted** | 미리보기 정리 → 사진 선택 → 삭제대기함으로 이동 | 동일 |

> cleanup 이벤트는 **즉시 전송** (session flush 아님). Home 버튼은 네트워크 전송 완료를 기다리는 안정화 목적으로 권장.

---

## 4. 구현: accessibilityIdentifier 추가

### 4.1 ViewerViewController (뷰어 버튼 4개)

**파일**: `PickPhoto/PickPhoto/Features/Viewer/ViewerViewController.swift`

| 요소 | identifier | 추가 위치 |
|------|-----------|----------|
| 뒤로 버튼 | `"viewer_back"` | `setupBackButton()` (588줄 근처) |
| 삭제 버튼 | `"viewer_delete"` | 버튼 생성 시점 (169줄 근처) |
| 복구 버튼 | `"viewer_restore"` | 버튼 생성 시점 (178줄 근처) |
| 완전삭제 버튼 | `"viewer_permanent_delete"` | 버튼 생성 시점 (187줄 근처) |

### 4.2 FaceComparisonViewController (비교 화면 버튼 2개)

**파일**: `PickPhoto/PickPhoto/Features/SimilarPhoto/UI/FaceComparisonViewController.swift`

| 요소 | identifier | 추가 위치 |
|------|-----------|----------|
| 취소 버튼 | `"comparison_cancel"` | 버튼 생성 시점 |
| 삭제 버튼 | `"comparison_delete"` | 버튼 생성 시점 |

### 4.3 기존 접근성으로 충분한 요소

| 요소 | 접근 방법 | 비고 |
|------|----------|------|
| 탭바 버튼 | `app.buttons["삭제대기함"]` | LiquidGlassTabButton accessibilityLabel |
| +버튼 | `app.buttons.matching(NSPredicate(format: "label CONTAINS '비교'"))` | FaceButtonOverlay accessibilityLabel |
| 그리드 셀 | `collection.cells.element(boundBy: index)` | 인덱스 기반 |

---

## 5. 구현: XCUITest

**신규 파일**: `PickPhoto/PickPhotoUITests/AnalyticsUITest.swift` (~250줄)

### 5.1 테스트 클래스 구조

```swift
import XCTest

final class AnalyticsUITest: XCTestCase {

    private var app: XCUIApplication!

    override func setUpWithError() throws {
        continueAfterFailure = false
        app = XCUIApplication()

        // 권한 다이얼로그 자동 허용 (실기기 최초 실행 시)
        addUIInterruptionMonitor(withDescription: "Photos Permission") { alert in
            for label in ["모든 사진에 대한 접근 허용", "Allow Full Access", "허용", "Allow"] {
                let button = alert.buttons[label]
                if button.exists { button.tap(); return true }
            }
            return false
        }

        // 완전삭제 시 iOS 시스템 확인 다이얼로그 자동 허용
        // PHPhotoLibrary.performChanges → "N장의 사진을 삭제하도록 허용하겠습니까?" 팝업
        addUIInterruptionMonitor(withDescription: "Photo Deletion") { alert in
            for label in ["삭제", "Delete"] {
                let button = alert.buttons[label]
                if button.exists { button.tap(); return true }
            }
            return false
        }

        app.launch()

        let collection = app.collectionViews.firstMatch
        XCTAssertTrue(collection.waitForExistence(timeout: 10), "그리드 로드 실패")
        // app.tap() 제거: 권한이 설정에서 이미 허용된 상태이므로 팝업이 없음
        // app.tap()이 있으면 그리드 셀을 탭해 카운터가 오염될 수 있음
    }

    func testAnalyticsCounters() throws {
        phase1_similarPhoto()
        phase2_photoViewing()
        phase3_viewerDelete()
        phase4_gridSwipeDelete()
        phase5_trashViewer()

        // Phase 6: Home → background → flush
        XCUIDevice.shared.press(.home)
        Thread.sleep(forTimeInterval: 5)
    }
}
```

### 5.2 Phase 1: 유사 사진

```swift
private func phase1_similarPhoto() {
    let collection = app.collectionViews.firstMatch

    // Step 1: 유사 사진 셀 탭 → 뷰어
    collection.cells.element(boundBy: 0).tap()

    // Step 2: +버튼 대기 → 탭
    let faceButton = app.buttons.matching(
        NSPredicate(format: "label CONTAINS '비교'")
    ).firstMatch
    XCTAssertTrue(faceButton.waitForExistence(timeout: 5), "+버튼 미표시")
    faceButton.tap()

    // Step 3: 비교 화면에서 셀 2개 선택
    // - 초기 상태: 5장 전부 미선택 (체크마크 없음)
    // - 셀 탭 → 체크마크 + 파란 오버레이 표시 (선택 토글)
    // - 2개 선택 후 삭제 버튼 활성화됨
    // - 주의: 비교 화면이 modal로 뷰어 위에 올라오므로 collectionViews 순서 확인 필요
    //   (firstMatch가 메인 그리드를 잡을 경우 lastMatch 또는 index 조정)
    let comparisonGrid = app.collectionViews.firstMatch
    XCTAssertTrue(comparisonGrid.waitForExistence(timeout: 3), "비교 화면 로드 실패")
    Thread.sleep(forTimeInterval: 0.5)  // 렌더링 대기
    comparisonGrid.cells.element(boundBy: 0).tap()  // 셀 0 선택 → 체크마크
    Thread.sleep(forTimeInterval: 0.3)
    comparisonGrid.cells.element(boundBy: 1).tap()  // 셀 1 선택 → 체크마크
    Thread.sleep(forTimeInterval: 0.3)

    // Step 4: 삭제 버튼 탭 → 비교 화면 + 뷰어 dismiss → 그리드 복귀
    let deleteBtn = app.buttons["comparison_delete"]
    XCTAssertTrue(deleteBtn.exists, "비교 화면 삭제 버튼 없음")
    deleteBtn.tap()

    // Step 5: 그리드 복귀 확인
    XCTAssertTrue(collection.waitForExistence(timeout: 5), "그리드 복귀 실패")
    Thread.sleep(forTimeInterval: 0.5)
}
```

### 5.3 Phase 2: 사진 열람

```swift
private func phase2_photoViewing() {
    let collection = app.collectionViews.firstMatch

    // Step 6: 셀 탭 → 뷰어
    collection.cells.element(boundBy: 0).tap()
    let deleteButton = app.buttons["viewer_delete"]
    XCTAssertTrue(deleteButton.waitForExistence(timeout: 5), "뷰어 열림 실패")

    // Step 7-11: 왼쪽 스와이프 5회
    for _ in 0..<5 {
        app.swipeLeft()
        Thread.sleep(forTimeInterval: 0.5)
    }

    // Step 12: 뒤로 → 닫기
    app.buttons["viewer_back"].tap()
    XCTAssertTrue(collection.waitForExistence(timeout: 5), "그리드 복귀 실패")
}
```

### 5.4 Phase 3: 뷰어 삭제

```swift
private func phase3_viewerDelete() {
    let collection = app.collectionViews.firstMatch

    // Step 13: 셀 탭 → 뷰어
    collection.cells.element(boundBy: 0).tap()
    let deleteButton = app.buttons["viewer_delete"]
    XCTAssertTrue(deleteButton.waitForExistence(timeout: 5))

    // Step 14-16: 삭제 버튼 3회
    for _ in 0..<3 {
        deleteButton.tap()
        Thread.sleep(forTimeInterval: 0.8)
    }

    // Step 17-18: 위로 스와이프 2회
    for _ in 0..<2 {
        app.swipeUp(velocity: .fast)
        Thread.sleep(forTimeInterval: 0.8)
    }

    // Step 19: 뒤로 → 닫기
    app.buttons["viewer_back"].tap()
    XCTAssertTrue(collection.waitForExistence(timeout: 5))
}
```

### 5.5 Phase 4: 목록에서 밀어서 삭제

```swift
private func phase4_gridSwipeDelete() {
    let collection = app.collectionViews.firstMatch

    // Step 20-23: 셀 수평 스와이프 4회
    for i in 0..<4 {
        let cell = collection.cells.element(boundBy: 0)
        guard cell.waitForExistence(timeout: 3) else {
            XCTFail("셀 \(i) 접근 실패"); return
        }
        let start = cell.coordinate(withNormalizedOffset: CGVector(dx: 0.8, dy: 0.5))
        let end = cell.coordinate(withNormalizedOffset: CGVector(dx: 0.1, dy: 0.5))
        start.press(forDuration: 0.05, thenDragTo: end, withVelocity: 1500, thenHoldForDuration: 0)
        Thread.sleep(forTimeInterval: 1.0)
    }
}
```

### 5.6 Phase 5: 삭제대기함

```swift
private func phase5_trashViewer() {
    // Step 24: 삭제대기함 탭
    let trashTab = app.buttons["삭제대기함"]
    XCTAssertTrue(trashTab.waitForExistence(timeout: 3), "삭제대기함 탭 찾기 실패")
    trashTab.tap()

    let collection = app.collectionViews.firstMatch
    XCTAssertTrue(collection.waitForExistence(timeout: 5), "삭제대기함 로드 실패")

    // Step 25: 셀 탭 → 뷰어
    collection.cells.element(boundBy: 0).tap()
    let permanentDeleteBtn = app.buttons["viewer_permanent_delete"]
    XCTAssertTrue(permanentDeleteBtn.waitForExistence(timeout: 5), "삭제대기함 뷰어 열림 실패")

    // Step 26-27: 완전삭제 2회
    for _ in 0..<2 {
        permanentDeleteBtn.tap()
        Thread.sleep(forTimeInterval: 1.0)
    }

    // Step 28-30: 복구 3회
    let restoreBtn = app.buttons["viewer_restore"]
    for _ in 0..<3 {
        restoreBtn.tap()
        Thread.sleep(forTimeInterval: 0.8)
    }

    // Step 31: 뒤로 → 닫기
    let backButton = app.buttons["viewer_back"]
    if backButton.exists { backButton.tap() }
}
```

---

## 6. 구현: 실행 스크립트

**신규 파일**: `scripts/analytics/run-test-b.sh` (~30줄)

```bash
#!/bin/bash
# Analytics Test B: 실기기 XCUITest E2E → Supabase 검증
set -euo pipefail
cd "$(dirname "$0")/../.."  # 프로젝트 루트

DEVICE_ID="00008110-00041DDC212A801E"
DEVICE_NAME="iPhone 13Pro128"

# 테스트 시작 시각 캡처 (verify에 전달해 오검출 방지)
TEST_START=$(date -u '+%Y-%m-%dT%H:%M:%SZ')

echo "=== Step 1: 기기 연결 확인 ==="
xcrun xctrace list devices 2>&1 | grep "$DEVICE_NAME" || {
    echo "❌ $DEVICE_NAME 미연결. USB 연결 확인 필요."
    exit 1
}

echo ""
echo "=== Step 2: 빌드 + 테스트 실행 ==="
xcodebuild test \
  -project PickPhoto/PickPhoto.xcodeproj \
  -scheme PickPhoto \
  -destination "platform=iOS,id=$DEVICE_ID" \
  -configuration Debug \
  -only-testing:PickPhotoUITests/AnalyticsUITest/testAnalyticsCounters \
  2>&1 | tail -20

echo ""
echo "=== Step 3: Supabase 검증 (시작 시각 기준) ==="
# 테스트 시작 시각을 verify에 전달 → "최근 2분" 대신 정확한 기준으로 오검출 방지
scripts/analytics/verify-test-b.sh "$TEST_START"
```

---

## 7. 구현: 검증 스크립트

**신규 파일**: `scripts/analytics/verify-test-b.sh` (~120줄)

```bash
#!/bin/bash
# Analytics Test B: 실기기 XCUITest E2E 검증
# 사용법:
#   ./verify-test-b.sh [TEST_START]   # 자동 테스트 검증 (시작 시각 인자 선택)
#   ./verify-test-b.sh --manual       # 수동 테스트 검증 (cleanup 이벤트만 확인)
set -euo pipefail

source "$(dirname "$0")/.env"

# --manual 모드: cleanup 이벤트만 확인 (자동 항목 실행 안 함)
if [ "${1:-}" = "--manual" ]; then
    SINCE=$(date -u -v-5M '+%Y-%m-%dT%H:%M:%SZ' 2>/dev/null \
         || date -u -d '5 minutes ago' '+%Y-%m-%dT%H:%M:%SZ')
    BASE="$SUPABASE_URL/rest/v1/events"
    AUTH=(-H "apikey: $SUPABASE_SERVICE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_KEY")
    PASS=0; FAIL=0
    check_exists_and_dump() {
        local event="$1"
        local raw
        raw=$(curl -fsS "$BASE?event_name=eq.$event&created_at=gte.$SINCE&order=created_at.desc&limit=1" \
          "${AUTH[@]}" 2>&1) || { printf "  ❌ %-40s FETCH ERROR\n" "$event"; FAIL=$((FAIL+1)); return; }
        if [ "$(echo "$raw" | jq 'length')" -eq 0 ]; then
            printf "  ❌ %-40s 미존재\n" "$event"; FAIL=$((FAIL+1)); return
        fi
        printf "  ✅ %-40s 존재\n" "$event"; PASS=$((PASS+1))
        echo "$raw" | jq '.[0].params' | sed 's/^/     /'
    }
    echo "=== 수동 테스트: 정리 이벤트 확인 ==="
    echo "    (cleanup 이벤트는 즉시 전송 — Home 버튼은 전송 완료 안정화용)"
    echo ""
    echo "[cleanup.completed]"; check_exists_and_dump "cleanup.completed"
    echo ""; echo "[cleanup.previewCompleted]"; check_exists_and_dump "cleanup.previewCompleted"
    echo ""; echo "결과: $PASS 통과 / $FAIL 실패"
    [ "$FAIL" -eq 0 ] && exit 0 || exit 1
fi

# 자동 테스트 모드: 시작 시각 인자 또는 기본값(2분 전) 사용
# run-test-b.sh에서 TEST_START를 전달해 오검출 방지
if [ -n "${1:-}" ]; then
    SINCE="$1"
else
    SINCE=$(date -u -v-2M '+%Y-%m-%dT%H:%M:%SZ' 2>/dev/null \
         || date -u -d '2 minutes ago' '+%Y-%m-%dT%H:%M:%SZ')
fi

# Supabase 전송 완료 대기 (최대 30초 polling)
echo "=== Supabase 전송 완료 대기 중 (최대 30초) ==="
BASE_TMP="$SUPABASE_URL/rest/v1/events"
AUTH_TMP=(-H "apikey: $SUPABASE_SERVICE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_KEY")
for i in $(seq 1 6); do
    COUNT=$(curl -fsS "$BASE_TMP?event_name=eq.session.photoViewing&created_at=gte.$SINCE&limit=1" \
      "${AUTH_TMP[@]}" 2>/dev/null | jq 'length' 2>/dev/null || echo 0)
    if [ "$COUNT" -gt 0 ]; then
        echo "  데이터 수신 확인 (${i}번째 시도)"
        break
    fi
    echo "  대기 중... (${i}/6)"
    sleep 5
done

BASE="$SUPABASE_URL/rest/v1/events"
AUTH=(-H "apikey: $SUPABASE_SERVICE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_KEY")

PASS=0; FAIL=0

sb_fetch() {
    local event="$1"
    local raw
    raw=$(curl -fsS "$BASE?event_name=eq.$event&created_at=gte.$SINCE&order=created_at.desc&limit=1" \
      "${AUTH[@]}" 2>&1) || { echo "FETCH_ERROR"; return; }
    if echo "$raw" | jq -e 'if type == "array" then true else false end' >/dev/null 2>&1; then
        echo "$raw"
    else
        echo "FETCH_ERROR"
    fi
}

check() {
    local event="$1" key="$2" expected="$3"
    local data actual
    data=$(sb_fetch "$event")
    if [ "$data" = "FETCH_ERROR" ]; then
        printf "  ❌ %-40s FETCH ERROR\n" "$event.$key"
        FAIL=$((FAIL + 1)); return
    fi
    actual=$(echo "$data" | jq -r ".[0].params.\"$key\" // empty")
    if [ "$actual" = "$expected" ]; then
        printf "  ✅ %-40s = %s\n" "$event.$key" "$actual"
        PASS=$((PASS + 1))
    else
        printf "  ❌ %-40s = %s (기대: %s)\n" "$event.$key" "${actual:-NULL}" "$expected"
        FAIL=$((FAIL + 1))
    fi
}

# 값이 N 이상인지 확인 (≥ 비교)
check_gte() {
    local event="$1" key="$2" min="$3"
    local data actual
    data=$(sb_fetch "$event")
    if [ "$data" = "FETCH_ERROR" ]; then
        printf "  ❌ %-40s FETCH ERROR\n" "$event.$key"
        FAIL=$((FAIL + 1)); return
    fi
    actual=$(echo "$data" | jq -r ".[0].params.\"$key\" // empty")
    if [ -n "$actual" ] && [ "$actual" -ge "$min" ] 2>/dev/null; then
        printf "  ✅ %-40s = %s (≥ %s)\n" "$event.$key" "$actual" "$min"
        PASS=$((PASS + 1))
    else
        printf "  ❌ %-40s = %s (기대: ≥ %s)\n" "$event.$key" "${actual:-NULL}" "$min"
        FAIL=$((FAIL + 1))
    fi
}

echo "=== Analytics Test B: 실기기 E2E 검증 ==="
echo "    조회 범위: $SINCE 이후"
echo ""

echo "[session.photoViewing]"
check "session.photoViewing" "total"       "9"
check "session.photoViewing" "fromLibrary" "8"
check "session.photoViewing" "fromAlbum"   "0"
check "session.photoViewing" "fromTrash"   "1"

echo ""
echo "[session.deleteRestore]"
check "session.deleteRestore" "gridSwipeDelete"     "4"
check "session.deleteRestore" "gridSwipeRestore"    "0"
check "session.deleteRestore" "viewerSwipeDelete"   "2"
check "session.deleteRestore" "viewerTrashButton"   "3"
check "session.deleteRestore" "viewerRestoreButton" "3"
check "session.deleteRestore" "fromLibrary"         "9"
check "session.deleteRestore" "fromAlbum"           "0"

echo ""
echo "[session.trashViewer]"
check "session.trashViewer" "permanentDelete" "2"
check "session.trashViewer" "restore"         "3"

echo ""
echo "[similar.groupClosed]"
check "similar.groupClosed" "totalCount"   "5"
check "similar.groupClosed" "deletedCount" "2"

echo ""
echo "[session.similarAnalysis]"
check_gte "session.similarAnalysis" "completedCount" "1"
check_gte "session.similarAnalysis" "totalGroups"    "1"

echo ""
echo "======================================="
echo "  결과: $PASS 통과 / $FAIL 실패 (총 $((PASS + FAIL)) 항목)"
echo "======================================="

[ "$FAIL" -eq 0 ] && exit 0 || exit 1
```

---

## 8. 파일 변경 요약

| 구분 | 파일 | 예상 줄 | 비고 |
|------|------|:------:|------|
| **신규** | `PickPhoto/PickPhotoUITests/AnalyticsUITest.swift` | ~250줄 | XCUITest (6 Phase) |
| **신규** | `scripts/analytics/run-test-b.sh` | ~30줄 | 실행 스크립트 |
| **신규** | `scripts/analytics/verify-test-b.sh` | ~120줄 | 검증 스크립트 (자동+수동) |
| **수정** | `PickPhoto/.../ViewerViewController.swift` | +4줄 | accessibilityIdentifier 4개 |
| **수정** | `PickPhoto/.../FaceComparisonViewController.swift` | +2줄 | accessibilityIdentifier 2개 |

**총 신규 3개 / 수정 2개**

---

## 9. 주의사항 & 확인 필요

### 9.1 실기기 테스트 주의사항

| # | 항목 | 설명 |
|---|------|------|
| 1 | **완전삭제 위험** | Phase 5에서 permanentDelete 2회 → 실제 iOS Photos에서 영구 삭제. 테스트용 사진만 사용 |
| 2 | **사진 세팅 순서** | 일반 15장 먼저 → 인물 셀카 5장 나중에 (최신 사진이 그리드 맨 앞) |
| 3 | **코치마크 완료** | 코치마크 미완료 시 오버레이가 XCUITest 동작을 차단 |
| 4 | **USB 연결 유지** | 테스트 실행 중 USB 분리 시 테스트 실패 |
| 5 | **Supabase 활성** | 7일 미사용 시 자동 pause → Dashboard에서 Resume |

### 9.2 구현 시 확인 필요

| # | 항목 | 확인 방법 |
|---|------|----------|
| 1 | `setViewControllers(animated:true)` → `didFinishAnimating` 호출 여부 | Apple 문서상 미호출이 표준. 호출되면 photoViewing 기대값 조정 |
| 2 | 그리드 스와이프 제스처가 `press(thenDragTo:withVelocity:)` 에 반응하는지 | 좌표, 속도 조정 필요할 수 있음 |
| 3 | 뷰어 위로 스와이프 `.swipeUp(velocity: .fast)`가 삭제 임계값 충족하는지 | 미충족 시 좌표 기반 드래그로 변경 |
| 4 | permanentDelete 비동기 완료 대기 | `Thread.sleep(1.0)` 조정 가능 |
| 5 | `app.buttons["삭제대기함"]` 정상 탐색 여부 | LiquidGlassTabButton accessibilityLabel 확인됨 |
| 6 | 비교 화면 collectionView 셀 탭이 정상 동작하는지 | 셀 선택 토글 확인 |
| 7 | 비교 화면 삭제 후 뷰어+비교화면 자동 dismiss 동작 확인 | `faceComparisonViewController(_:didDeletePhotos:)` 경로 |
| 8 | `Thread.sleep` → `waitForExistence` 전환 여부 | 실기기 애니메이션 속도 따라 flaky 발생 시 각 단계 sleep을 predicate 기반 대기로 교체 |
| 9 | 비교화면 `collectionViews.firstMatch` 가 modal 위 컬렉션을 정확히 잡는지 | 잘못 잡으면 `collectionViews.lastMatch` 또는 별도 accessibilityIdentifier 추가 필요 |

### 9.3 테스트 A · B 비교

| 항목 | 테스트 A | 테스트 B (자동) | 테스트 B (수동) |
|------|---------|:-------------:|:-------------:|
| **타겟** | 시뮬레이터 | 실기기 | 실기기 |
| **검증 경로** | 코드 주입 → flush | UI 동작 → 카운터 → flush | 수동 동작 → flush |
| **검증 범위** | 10개 이벤트 (40항목) | 5개 이벤트 (18항목) | 2개 이벤트 (존재 확인) |
| **기대값** | 임의 고유값 | 실제 동작 횟수 | 존재 여부만 |
| **목적** | 전송 경로 검증 | 수집 경로 검증 | 수집 경로 검증 |

### 9.4 수동 테스트 절차 (정리)

1. XCUITest 완료 후, 앱을 다시 실행
2. 정리 플로우 수행 (cleanup.completed 또는 cleanup.previewCompleted)
3. **Home 버튼** → 네트워크 전송 완료 대기 (3~5초) — cleanup은 즉시 전송이므로 flush 불필요
4. `scripts/analytics/verify-test-b.sh --manual` 실행
6. 출력된 파라미터 값을 육안으로 확인
