# iOS 26 정리버튼 랙 원인 분석 및 해결 계획

**날짜**: 2026-02-12
**증상**: iOS 26에서 정리버튼을 **처음 1회 클릭 시** UI 랙이 심함 (2회차부터는 발생하지 않음)

## 완료 기준

1. 최초 클릭 후 **0.5초 내** 진행 UI가 반드시 1회 이상 갱신된다
2. 최초 클릭 구간에서 **Main Thread 100ms 이상 stall이 0회**다
3. 최초 1회 총 분석시간이 기존 대비 **최소 30% 감소**한다
4. 1회차/2회차 간 체감 편차(모델 초기화 지연)가 크게 줄어든다

---

## 근본 원인 구조도

```
GridViewController (UIViewController → @MainActor)
    │
    └─ Task { }  ← MainActor 상속 (문제의 시작점)
         │
         ├─ CleanupPreviewService.analyze()     ← 호출자 executor 상속
         │    ├─ QualityAnalyzer.analyze()       ← MainActor에서 실행
         │    │    ├─ imageLoader.loadImage()       (async, 콜백 기반)
         │    │    ├─ exposureAnalyzer.analyze()    (동기, CPU)
         │    │    ├─ blurAnalyzer.analyze()        (동기, Metal/CPU)
         │    │    └─ detectTextScreenshot()        (동기 블로킹!)
         │    │         └─ VNImageRequestHandler.perform() ← 메인 스레드 차단
         │    │
         │    ├─ CleanupImageLoader.loadImage()  ← 중복 로드
         │    ├─ AestheticsAnalyzer.analyze()    ← ML 추론 (무거움)
         │    └─ detectTextScreenshot()          ← 동기 블로킹 (중복 호출!)
         │
         └─ CleanupService.startCleanup()       ← 같은 문제
              └─ QualityAnalyzer.analyzeBatch()  ← TaskGroup도 MainActor 상속
```

## 핵심 원인: `Task { }`가 MainActor를 상속하여 모든 분석이 메인 스레드에서 실행됨

`GridViewController+Cleanup.swift:353`에서 생성되는 `Task { }`가 문제의 시작점.

```swift
// GridViewController+Cleanup.swift:353
Task {
    let result = try await service.analyze(...)
}
```

`GridViewController`는 `UIViewController`의 하위 클래스이므로 `@MainActor`로 격리됨.
이 안에서 생성된 `Task { }`는 MainActor 컨텍스트를 상속받아, 모든 분석 코드가 메인 스레드에서 실행됨.

### 빌드 설정 주의: `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`

`project.pbxproj`에 Debug/Release 모두 아래 설정이 활성화되어 있음:
```
SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor;
SWIFT_APPROACHABLE_CONCURRENCY = YES;
```

이 설정은 **explicit actor isolation 어노테이션이 없는 모든 선언을 암묵적 `@MainActor`로 취급**함.
`CleanupPreviewService`, `CleanupService`, `QualityAnalyzer`, `SafeGuardChecker` 모두:
- PickPhoto 앱 타깃 소속 (AppCore 패키지 아님)
- explicit actor isolation 어노테이션 없음 (`@MainActor`, `nonisolated` 등 미사용)

따라서 이 클래스들의 모든 메서드가 **암묵적 `@MainActor`**가 됨.

그러나 **실제 UI 프리즈를 유발하는 것은 동기 Vision API 호출 3곳**뿐임:
- 나머지 분석 작업(이미지 로드, 노출/블러 분석, AestheticsScore)은 `async`이거나 360px 이미지 기준 ~5-15ms의 경량 동기 작업
- `async` 함수의 `await` 지점마다 MainActor가 UI 이벤트를 처리할 수 있어 프리즈는 발생하지 않음
- `await Task.yield()` 호출로 배치 간 양보도 이미 구현되어 있음

> **결론**: `nonisolated` 전환이나 `Task.detached` 없이, **동기 Vision 호출만 백그라운드로 전환**(`VisionExecutor`)하면 메인 스레드 블로킹이 해소됨

## 처음 1회만 발생하는 이유: Vision 모델 Lazy Initialization

Vision Framework의 ML 모델은 **첫 호출 시에만 로드**되고, 이후 프로세스 메모리에 상주함:

| API | 사용 위치 | 1회차 (모델 로드) | 2회차 이후 |
|-----|----------|-----------------|---------|
| `CalculateImageAestheticsScoresRequest` | AestheticsAnalyzer.swift:73 | ~300-500ms | ~30-50ms |
| `VNRecognizeTextRequest` | CleanupPreviewService.swift:466, QualityAnalyzer.swift:379 | ~200-300ms | ~20-30ms |
| `VNDetectFaceCaptureQualityRequest` | SafeGuardChecker.swift:90 | ~200-300ms | ~20-30ms |

첫 사진 분석 시 모델 로드 비용 합계: **~700-1100ms** (메인 스레드에서 실행되므로 체감 프리즈)
2회차부터는 모델이 메모리에 남아 있어 분석만 수행 → 랙 없음

> **추가 악화 요인**: `detectTextScreenshot()`이 `CleanupPreviewService`와 `QualityAnalyzer`에서 **중복 호출**되어 모델 로드 비용도 중복 발생 가능

---

## iOS 26에서 특히 심한 이유

**iOS 16~17 경로** (가벼움):
- `QualityAnalyzer.analyze(asset)` → 이미지 로드 + 노출/블러 분석만

**iOS 18+ 경로** (iOS 26 포함, 매우 무거움):
- `QualityAnalyzer.analyze(asset)` → 이미지 로드 + 노출/블러 분석
- `CleanupImageLoader.loadImage(for: asset)` → 두 번째 이미지 로드 (중복!)
- `AestheticsAnalyzer.analyze(image)` → `CalculateImageAestheticsScoresRequest.perform(on:)` (무거운 ML 추론)
- `detectTextScreenshot(image)` → `VNImageRequestHandler.perform([request])` (동기 블로킹!)

사진 1장당 작업량이 3~4배 증가.

## 구체적인 블로킹 지점

### 1. `CleanupPreviewService.swift:484` - detectTextScreenshot 내부

```swift
let handler = VNImageRequestHandler(cgImage: image, options: [:])
try handler.perform([request])  // ← 동기 함수, 메인 스레드 완전 블로킹
```

`withCheckedContinuation` 안이지만, continuation 클로저 자체가 MainActor 스레드에서 동기 실행되므로 `perform()`이 메인 스레드를 차단.

### 2. `AestheticsAnalyzer.swift:77` - AestheticsScore 분석

```swift
let observation = try await request.perform(on: image)  // ← async지만 MainActor에서 resume
```

`async`이므로 완전 블로킹은 아니지만, resume이 MainActor에서 일어나 다른 UI 작업을 지연시킴.

### 3. `QualityAnalyzer.swift:429` - detectTextScreenshot (중복)

QualityAnalyzer 내부에도 동일한 `detectTextScreenshot`이 있어 동기 블로킹 발생.

### 4. `SafeGuardChecker.swift:93` - checkFaceQuality

```swift
let handler = VNImageRequestHandler(cgImage: image, options: [:])
try handler.perform([request])  // 동기 호출
```

### 추가 악화 요인

- `CleanupPreviewService.swift:212` - iOS 18+ 경로에서 `CleanupImageLoader.loadImage()`를 한 번 더 호출 (QualityAnalyzer가 이미 로드했는데 또 로드)
- 50장 루프 (`debugLightLimit`) 동안 누적 → 각 사진당 수백ms x 50장 = 수초~수십초 메인 스레드 점유
- `progressView.update(with: progress)` UI 업데이트도 분석이 메인 스레드를 차지하고 있어 프로그레스 UI도 갱신되지 않음 (화면 프리즈)

### 같은 문제가 있는 다른 코드

- `startCleanup` (`GridViewController+Cleanup.swift:172`)도 동일한 패턴의 `Task { }` 사용
- DEBUG 전용 `startIntegratedLogicTest` (`GridViewController+Cleanup.swift:444`)
- DEBUG 전용 `startModeComparisonTest` (`GridViewController+Cleanup.swift:540`)

---

## 해결 전략

### 왜 `nonisolated` / `Task.detached`가 불필요한가

초기 분석에서는 전체 분석 체인을 MainActor에서 분리하는 방안(nonisolated 전환 + Task.detached)을 검토했으나, 아래 이유로 **채택하지 않음**:

1. **실제 블로킹은 동기 Vision 호출 3곳뿐**. 나머지 async 작업은 `await` 지점에서 MainActor가 UI를 처리할 수 있어 프리즈를 유발하지 않음
2. **nonisolated 전환 시 부작용이 큼**:
   - `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`로 인해 저장 프로퍼티도 암묵적 `@MainActor` → nonisolated 메서드에서 접근 시 **컴파일 오류**
   - `CleanupService`의 `currentSession`, `foundAssetIDs` 등 lock 범위 밖 접근 → **데이터 레이스**
   - `Task.detached`에서 non-Sendable 타입(서비스, UIView) 캡처 → **컴파일 오류**
   - `Task.detached`는 Task 취소 미상속 → **리소스 낭비**
   - progressHandler의 메인 스레드 보장 깨짐 → **UIKit 크래시**
3. **수정 범위가 9개 파일로 확대**되어 회귀 리스크가 높음

### 권장: VisionExecutor + 워밍업 (서비스 내부 자기 보호)

동기 Vision 호출만 공통 유틸로 백그라운드 전환. 기존 서비스 구조는 그대로 유지.

```swift
// VisionExecutor.swift (신규) — 동기 Vision 호출을 항상 백그라운드에서 실행
enum VisionExecutor {
    static func performTextRecognition(on image: CGImage) async -> [VNRecognizedTextObservation] {
        await withCheckedContinuation { continuation in
            DispatchQueue.global(qos: .userInitiated).async {
                let request = VNRecognizeTextRequest()
                let handler = VNImageRequestHandler(cgImage: image, options: [:])
                try? handler.perform([request])
                let results = request.results ?? []
                continuation.resume(returning: results)
            }
        }
    }

    static func performFaceQuality(on image: CGImage) async -> [VNFaceObservation] { ... }
}
```

| 항목 | 평가 |
|------|------|
| 수정 범위 | 신규 2파일 + 기존 3파일의 Vision 호출부 교체 + 워밍업 연결 1곳 |
| 즉효성 | **완전 해결** — 동기 블로킹 3곳이 백그라운드로 이동하여 메인 스레드 즉시 해방 |
| 근본적인가 | **그렇다** — 누가 어디서 호출해도 동기 Vision 호출이 메인 스레드를 차단하지 않음 |
| 리스크 | **낮음** — 기존 actor isolation, 공유 상태, 콜백 패턴 전부 그대로 유지 |
| 부작용 | **없음** — nonisolated/Task.detached 미사용으로 컴파일 오류, 데이터 레이스, Sendable 문제 회피 |

### 왜 이것만으로 충분한가

```
사진 1장 분석 흐름 (MainActor에서 실행):

loadImage(for: asset)              ← async, await 지점 → UI 업데이트 가능
exposureAnalyzer.analyze(image)    ← 동기 ~5ms (360px) → OK
blurAnalyzer.analyze(image)        ← 동기 ~10ms (360px) → OK
VisionExecutor.performText(image)  ← async (백그라운드 실행), await 지점 → UI 업데이트 가능
AestheticsAnalyzer.analyze(image)  ← async (시스템 스레드), await 지점 → UI 업데이트 가능
VisionExecutor.performFace(image)  ← async (백그라운드 실행), await 지점 → UI 업데이트 가능
─────
await Task.yield()                 ← 배치 간 명시적 양보 (기존 코드)
```

동기 구간 최대 ~15ms (노출+블러) → 60fps 프레임 예산(16.67ms) 이내.
나머지는 모두 `await` 지점이므로 MainActor가 UI를 처리할 수 있음.

## 수정 대상 파일 목록

### 핵심: 신규 유틸 + Vision 호출 교체

| 파일 | 수정 내용 | 변경되지 않는 것 |
|------|-----------|-----------------|
| `VisionExecutor.swift` **(신규)** | 동기 Vision 호출 공통 캡슐화 — `performTextRecognition()`, `performFaceQuality()`. 내부에서 `DispatchQueue.global(qos: .userInitiated)` + `withCheckedContinuation` | — |
| `CleanupWarmupManager.swift` **(신규)** | Vision 모델 워밍업 싱글톤 — `warmUpIfNeeded()` 1회 보장 (NSLock + Bool). Aesthetics/Text/Face 모델 순차 초기화 | — |
| `CleanupPreviewService.swift:484` | `detectTextScreenshot` 내 `handler.perform()` → `VisionExecutor.performTextRecognition()` | actor isolation, 공유 상태, 콜백 패턴 전부 유지 |
| `QualityAnalyzer.swift:429` | `detectTextScreenshot` 내 `handler.perform()` → `VisionExecutor.performTextRecognition()` | actor isolation, mode, analyzeBatch 전부 유지 |
| `SafeGuardChecker.swift:93` | `checkFaceQuality` 내 `handler.perform()` → `VisionExecutor.performFaceQuality()` | actor isolation, 반환값 구조 유지 |

### 워밍업 연결

| 파일 | 수정 내용 |
|------|-----------|
| `GridViewController+Cleanup.swift` | `startPreviewCleanup`/`startCleanup` 시작 직전에 `await CleanupWarmupManager.warmUpIfNeeded()` 호출 추가 |

### 추가: 중복 제거 (강력 권장)

| 파일 | 수정 내용 |
|------|-----------|
| `CleanupPreviewService.swift:212` | `QualityAnalyzer`가 로드한 이미지를 재사용 (중복 이미지 로드 제거) |
| `CleanupPreviewService.swift:220` / `QualityAnalyzer.swift:429` | 텍스트 감지 결과를 1회 계산 후 공유 (중복 감지 제거) |

### 건드리지 않는 파일 (기존 구조 유지)

| 파일 | 유지 이유 |
|------|-----------|
| `CleanupService.swift` | actor isolation, `currentSession`/`foundAssetIDs` 접근 패턴 그대로 |
| `AestheticsAnalyzer.swift` | Vision async API 사용 → 이미 비블로킹 |
| `CleanupImageLoader.swift` | PHImageManager async 콜백 → 이미 비블로킹 |
| `VideoFrameExtractor.swift` | AVAssetImageGenerator async API → 이미 비블로킹 |
| `GridViewController+Cleanup.swift` | `Task { }` 유지 — 취소 상속, Sendable, progressHandler 스레드 안전 모두 유지 |

## 구현 순서

1. **`VisionExecutor.swift` 신규 작성**: `performTextRecognition()`, `performFaceQuality()` 구현
2. **`CleanupWarmupManager.swift` 신규 작성**: `warmUpIfNeeded()` 구현 (1회 보장)
3. **Vision 호출 교체** (3곳): `handler.perform()` → `VisionExecutor` 호출로 교체
4. **워밍업 연결**: `GridViewController+Cleanup.swift`에서 분석 시작 전 워밍업 호출 추가
5. **중복 제거**: 이미지 재사용 + 텍스트 감지 결과 공유

---

## 검증 계획

### 기능 검증

1. 최초 클릭 시 진행 UI가 즉시 갱신되는지
2. 취소 동작이 정상인지 (부분 이동 없음)
3. DEBUG 테스트 경로 2개(`startIntegratedLogicTest`, `startModeComparisonTest`)가 동일하게 동작하는지

### 성능 검증

1. Instruments Time Profiler로 최초 클릭 구간 측정
2. Main Thread call tree에서 `VNImageRequestHandler.perform`가 사라졌는지 확인
3. `os_signpost`로 아래 지표 수집:
   - tap → first progress update
   - tap → result
   - first run vs second run 비교

### 회귀 검증

1. iOS 16~17 경로(비 Aesthetics) 정상 동작
2. iOS 18+ 경로에서 분류 결과 개수 차이 과다 여부 확인
3. 취소 동작 정상 (기존 `Task { }` 유지이므로 취소 상속 정상 작동 확인)

---

## 리스크와 대응

| 리스크 | 심각도 | 대응 |
|--------|--------|------|
| 워밍업이 배터리/초기 부하 증가 | 낮음 | 1회 실행 + low priority + 앱 활성 상태에서만 수행 |
| iOS 메모리 압박 시 워밍업된 모델 퇴출 | 낮음 | 퇴출 시 첫 호출에서 다시 로드되나 백그라운드에서 실행되므로 UI 프리즈는 없음 |
| 중복 제거로 판정 결과가 달라질 수 있음 | 낮음 | 기존/신규 결과 샘플 500장 비교 로그로 오차 검증 |
| VisionExecutor 내 continuation 중복 resume | 낮음 | 기존 `hasResumed` 플래그 패턴을 VisionExecutor에도 동일 적용 |

---

## 의도적으로 하지 않는 것

| 접근법 | 하지 않는 이유 |
|--------|---------------|
| 서비스 메서드 `nonisolated` 전환 | 저장 프로퍼티 컴파일 오류, 데이터 레이스, lock 범위 확장 등 부작용이 큼 |
| `Task { }` → `Task.detached` 교체 | non-Sendable 캡처 오류, 취소 미상속, progressHandler 스레드 안전 깨짐 |
| `SWIFT_DEFAULT_ACTOR_ISOLATION` 변경 | 앱 전체 동시성 모델에 영향, 범위 과대 |

이 접근들은 문제의 근본 원인(동기 Vision 블로킹 3곳)과 무관하게 전체 서비스 구조를 바꾸는 것이라 **리스크 대비 이득이 작음**.

---

## 주의사항

- `VisionExecutor`의 `DispatchQueue.global()` 내부에서 `continuation.resume()`이 정확히 1회만 호출되도록 보장 (기존 `hasResumed` 패턴 유지)
- 워밍업은 `Task { }` (MainActor)에서 `await`으로 호출하되, 내부 실제 Vision 수행은 VisionExecutor 경유로 백그라운드 실행
- 중복 제거 시 `QualityAnalyzer`가 분석에 사용하는 이미지 크기(짧은 변 360px)와 `CleanupPreviewService`의 이미지 요구사항이 호환되는지 확인 필요
